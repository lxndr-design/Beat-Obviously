# Aether current-engine audit

Audit date: 2026-07-11  
Pinned revision: `062413553930fb102a81658063966c34500bf8a9` (`codex/aether-serum-foundation`)  
Scope: native Aether/Beat rendering code and existing stress/verifier surfaces. No DSP behavior was changed during this audit.

## Reproducibility and baseline

- Host: macOS 26.6, Apple Silicon arm64; Apple clang 21.0.0; CMake 4.3.2; Node 26.0.0; npm 11.12.1; JUCE 8.0.4.
- The first read-only run began on `aea0434223c54a413bdaff5f8adb8594acaf15ee`; the checkout changed externally during the run to clean revision `06241355`. Those first results are race-affected and are not the reproducible baseline.
- Fresh Release configuration, `Beat`, `BeatBackendStress`, and root `Beat.app` packaging succeeded in the isolated worktree; JUCE HarfBuzz emitted 20 third-party warnings. The packaged app registered `com.beat.project -> com.beat.app`.
- The initial checkout's `verify:non-native` failed in `verify:design-system` on eight pre-existing Drumpad CSS grid violations.
- The initial checkout's native stress runner failed after 1.53 s in persistence: `Recent project repository stress failed order=1 existence=0 legacyTimestamp=1 remove=1 record=1`. Later sections were not executed.
- The reproducible isolated `verify:non-native` run passed version verification for 0.2.1 and then failed on the same eight Drumpad design-grid violations. Later non-native gates were not executed.
- The reproducible isolated native run failed at the same persistence assertion after 1.53 s (1.10 s user, 0.04 s system). Peak RSS was 27,918,336 bytes and the reported peak memory footprint was 14,991,936 bytes. Later native sections were not executed.
- Individually running the fail-fast suite's remaining commands found two additional failures: `verify:audio-boundary` rejects direct `AudioContext` use in `DrumpadEditorModal.solid.tsx:393`, and `verify:daw` fails the left-split MIDI clipping assertion at `verify-daw-core.mjs:787`. Sampler zones, typecheck, synth roundtrip, document roundtrip, track interactions, frontend interactions, node instrument, drums, and the frontend production build passed.
- CPU brand and physical-memory queries were denied by the execution sandbox. These measurements remain unverified rather than inferred.

Because the existing complete gates are not green, this audit is a release blocker. No Milestone A DSP replacement may begin until the pinned baseline is rerun after packaging and the persistence failure is either fixed or explicitly accepted as unrelated.

## Claim verification

| Supplied claim | Finding | Evidence and consequence |
| --- | --- | --- |
| PolyBLEP saw and square | Verified | `BasicOscillator::sample()` applies `polyBlep()` at the saw discontinuity and both square edges. Triangle is naive and noise uses JUCE's process-global random source. |
| Independent timbral and harmonic-resolution axes | Resolved in Milestone A foundation | Position now selects adjacent immutable timbral frames only. Frequency/sample rate select adjacent harmonic mip levels only; playback crossfades continuously on both axes. |
| Linear runtime sample, frame, and mip interpolation | Verified | `readCurrentSample()` linearly interpolates adjacent samples, timbral frames, and logarithmically selected mip levels. No higher-quality offline path exists yet. |
| Mostly sample-rate-aware processing | Partial | Oscillator increments, ADSRs, filters, limiter release, glide, and LFO rates use the prepared rate. Several limits are constants or normalized policies; only existing stress coverage, not a systematic 44.1/48/88.2/96/192 kHz matrix, supports equivalence. |
| Caller-controlled parameter ramps | Verified | `RealtimeParameterChange` carries `rampSamples`; queue callers and automation events choose it. `InstrumentVoice` applies that value directly. There is no central parameter-rate/smoothing policy. |
| Partial click prevention | Verified | ADSR note tails, clip fades, sample fades, effect smoothing, and some project transitions exist. Hard voice stops call `clearCurrentNote()` without a shared steal/hard-stop fade, and routing/source/preset transitions do not share one de-click contract. |
| No always-on DC blocker | Verified | High-pass filters found in analysis/loudness metering do not process the audible master path. Wavetable construction does not establish a documented DC-removal invariant. |
| Final sample-peak limiter | Verified | `MasterLimiter` is the final safety stage and performs linked-channel instantaneous gain reduction plus an explicit sample clamp. It has no lookahead and is not a true-peak limiter. |
| Partial high-note and fast-pitch validation | Partial | Existing wavetable and high-polyphony stresses exercise pitch/render paths, but there is no automated alias-energy-by-note sweep, fast-FM/PM sideband threshold, or full sample-rate matrix. |
| Substantial but unproven real-time-safe memory handling | Verified | SPSC queues, fixed-capacity automation data, cached tables, immutable project snapshots, and preallocated recording buffers are present. There is no callback-wide allocation/lock/file-I/O detector. Shared-pointer release, JUCE callbacks, and container bounds are not comprehensively proven. |
| JUCE-controlled voice stealing | Verified | Beat only sets voice count and `setNoteStealingEnabled(true)` through `VoiceAllocation`; no Beat-owned victim selector implements released/quietest/oldest/stable-ID ordering. |
| Telemetry without overload response | Verified | Atomic render-work/cache counters and timing/meter data exist. Realtime parameter queue `push()` returns `false` on overflow, but there is no persistent overflow counter or explicit bounded overload policy. |
| No offline-only high-quality DSP mode | Verified | Offline export constructs another `AudioEngine` and calls the same callback/render path. Bit depth changes, but oscillator interpolation and nonlinear quality do not. |
| Stress tests lack comprehensive spectral regression | Verified | Existing tests cover render parity, limiter, caches, automation, high polyphony and effects. No reusable alias-energy, transition-spectrum, modulation-sideband, pitch-error, DC, or frequency-response threshold suite exists. |

## Additional material risks

- `InstrumentVoice::setParams()` obtains shared wavetable objects and can release prior `shared_ptr`s; the call sites must be proven non-callback or destruction must be deferred before claiming hard real-time safety.
- The runtime warp path uses a two-point nonlinear evaluation with a one-pole downsample state. It is described as oversampled but is not backed by a measured stop-band/alias specification.
- `BasicOscillator` noise uses `juce::Random::getSystemRandom()` while the Aether noise path has deterministic per-voice state. The standard oscillator path is therefore not guaranteed deterministic.
- Offline WAV export allocates buffers/vectors and performs file I/O by design outside a device callback; this is acceptable only because the offline entry is not a real-time thread.
- Existing telemetry counts work but does not prove allocation freedom, deadline compliance, or bounded destruction.

## Original baseline evidence requirements

These requirements are addressed by the stabilization report below. The recent-project semantic mismatch remains the sole explicit waiver, and Milestone A remains blocked for review.

## Baseline stabilization report — 2026-07-11

Milestone A remains blocked pending human review of this report. No production Aether DSP or upstream implementation was changed or imported.

### Snapshot discrepancy

`062413553930fb102a81658063966c34500bf8a9` cannot be certified as containing the complete earlier dirty working state. The first audit status captured many modified and untracked source files while HEAD was `aea04342`; the only committed delta from `aea04342` to `06241355` is the six-file version bump. Current reflogs contain only a later reset to `06241355`, and unreachable-object enumeration did not establish a safe mapping back to the missing working files. The baseline therefore proceeds from `06241355` with this explicit limitation.

The audit documents were frozen in snapshot commit `fcfc59f1` and tag `aether-audit-snapshot-2026-07-11`. The external bundle SHA-256 is `8a0a1d7139d5be67849be244983252d1e644b10045fc8bc6985e9744e276cbfc`.

### Gate classification

| Gate | Predates this Aether branch | Audio-engine validity | Disposition and evidence |
| --- | --- | --- | --- |
| Eight Drumpad 3px-grid violations | Yes; all lines blame to `aea04342` | None; CSS only | Fixed to 12/21/3/9px geometry. Design-system verifier passes. |
| Direct Drumpad `AudioContext` | Yes; line blamed to `aea04342` | Boundary validity only; no native DSP change | Fixed by using the existing Beat-owned timeline browser-preview context. Audio-boundary verifier passes. |
| DAW left-split MIDI clipping | Yes; split implementation dates to `a4666e84` | No oscillator/DSP impact; affects edit correctness and rendered note scheduling | Fixed production note-window clipping for split and origin-aware resize. Existing assertions were preserved; DAW verifier passes. |
| Recent-project existence stress | Yes; no-path-probe policy blamed to `aea04342` | None; persistence/startup metadata only | Temporarily waived by explicit environment flag. Production deliberately reports a non-empty stored path without filesystem probing to avoid macOS TCC prompts. The original assertion still fails and remains visible. |

### Complete gates

- `npm run verify:non-native`: passed end-to-end, including build.
- `Beat`, `BeatBackendStress`, and `BeatAetherBaseline`: built in Release.
- `AETHER_BASELINE_WAIVE_RECENT_PROJECT_EXISTS=1 build-native/bin/BeatBackendStress`: passed every section with only `baseline.recent-project-exists` waived.
- Native suite: 6.81 s wall, 5.08 s user, 0.80 s system; maximum RSS 194,297,856 bytes; reported peak footprint 180,814,544 bytes.

### Test-only spectral/render baseline

`BeatAetherBaseline` produced 150 WAVs: six scenarios × five sample rates (`44.1/48/88.2/96/192 kHz`) × five block sizes (`64/128/256/512/1024`). All five block-size renders for each scenario/rate share a hash, proving block-size determinism. Report hash: `38be9dfa47454ec6bb2c3e4ac490330229c62ffd993325c9843c8bba951c6830`; render-hash manifest: `488cff559576353b206afaf0bc041d6bcd23522b684effeb455e192ceab2fbad`.

- Initialization exposed a real sample-rate preparation defect: pitch error is about `-0.036 cents` at 44.1 kHz but rises to `146.68`, `1199.98`, `1346.71`, and `2546.72 cents` at 48/88.2/96/192 kHz. `prepare()` calls `setFrequency()` with an unchanged cached frequency, so the early return leaves the old 44.1-kHz phase delta.
- High-note pitch error remained between `-0.092` and `0.074 cents`; measured alias-energy ratio ranged `0.00297–0.01684`, DC stayed within `3.1e-9`, and maximum adjacent-sample step ranged `0.905–1.023`.
- Modulated/unison scenario pitch and alias fields are descriptive composite-signal measurements, not fundamental-frequency accuracy claims. Dense modulation DC ranged `-0.00264–0.01222`; rapid-automation maximum step reached `0.5693`.
- Harness time per 250-ms mono render ranged about `5.53–7.08 ms`; no coarse render deadline overruns were observed. Peak harness RSS was 14,450,688 bytes.
- Fixed-capacity queue probe accepted 64 events and rejected/counts 16 overflow events.
- The isolated oscillator harness bypasses the shared wavetable cache and records that fact explicitly. Existing full-engine stress supplies render-work and cache coverage; the harness does not fabricate those counters.

The existing timbre/mipmap-axis coupling remains a Milestone A input and is untouched.

## Sample-rate correctness and baseline freeze — 2026-07-11

No upstream code was imported, and no wavetable frame/mipmap redesign was started. The user subsequently approved Option A on 2026-07-11, establishing freeze commit `e74d6d99` as the canonical source baseline; separate phase and licensing gates remain in force.

### Root cause and correction

`WavetableOscillator::prepare()` previously called `setFrequency(frequencyHz)`. Because `setFrequency()` deliberately returns early when the numeric frequency is unchanged, preparing an oscillator at a new rate could retain the phase increment computed for 44.1 kHz. Preparation now always validates the active rate, clamps the cached frequency to the new Nyquist policy, and evaluates `phaseDelta = frequencyHz / sampleRate` directly. It marks the existing frame cache dirty but does not reset phase. `InstrumentVoice::prepare()` likewise recomputes its voice-owned `phaseDelta = baseFrequencyHz / sampleRate` before preparing the main, A/B-unison, and related oscillator banks. Stateless `BasicOscillator` and sub/noise calls already receive a rate-derived increment at their call sites; live and offline engines both propagate their active rate through voice preparation.

Focused native coverage verifies 44.1/48/88.2/96/192 kHz tuning, changed-rate recomputation with unchanged frequency, same-rate idempotence, preserved phase, before/after-prepare frequency-order equivalence, BasicOscillator tuning, and exact WavetableOscillator phase increments. The complete live/offline stress path and render matrix remain green.

### Frozen results

- `npm run verify:non-native`: passed end-to-end.
- `Beat`, `BeatBackendStress`, and `BeatAetherBaseline`: Release targets built.
- Native stress with only `baseline.recent-project-exists` waived: passed every section in 6.69 s wall / 5.07 s user / 0.76 s system; maximum RSS 210,075,648 bytes. Compared with the prior run this is -0.12 s wall and +15,777,792 bytes RSS; this single-run delta is descriptive, not a performance regression conclusion.
- Initialization pitch error before -> after (cents): 44.1 kHz `-0.0360 -> -0.0360`; 48 kHz `146.6803 -> -0.0132`; 88.2 kHz `1199.9821 -> -0.0360`; 96 kHz `1346.7135 -> -0.0132`; 192 kHz `2546.7217 -> -0.0132`.
- Of 150 deterministic WAVs, 130 are byte-identical to the pre-fix baseline. Exactly 20 changed: the initialization scenario at each non-44.1-kHz rate across five block sizes. All 30 44.1-kHz renders and all non-initialization scenarios are unchanged. These expected changes are accepted explicitly rather than silently replacing hashes.
- New JSON report SHA-256: `ac7d3bef75b98de5b69a9973dd2a8ac30549806190f2653fbef46345074b9231`; render manifest SHA-256: `c2a4a3632e39707473e91fe2889ea510fb1f2b1dfc16a71a35a699be072c621f`.
- Per-render time range: 5.495–7.204 ms (prior 5.533–7.080 ms). Harness wall time was 1.09 s; JSON peak RSS was 14,319,616 bytes and `/usr/bin/time` maximum RSS was 14,729,216 bytes. No deadline overruns were observed.
- Queue probe remains 64 accepted, 16 rejected, and 16 overflow. The isolated harness still bypasses the shared wavetable cache and does not invent unavailable full-engine render counters; full-engine stress remains the evidence for those paths.

The only waiver remains `baseline.recent-project-exists`. It suppresses only the stale path-existence result when the explicit environment variable is present; order, legacy timestamp, removal, recording, and every later persistence/native stress remain asserted and executed. The untested behavior is filesystem existence probing of stored recent-project paths, intentionally disabled to avoid macOS TCC prompts.

## Milestone A1 frame/mip foundation — 2026-07-11

Starting from canonical baseline `e74d6d99`, Beat now owns an immutable `TimbralFrame[]` model whose frames contain ordered immutable `MipLevel[]` sample vectors and explicit harmonic limits. Construction validates empty frames, missing or inconsistent levels, sample sizes, harmonic ordering/ranges, cross-frame harmonic-layout consistency, and finite samples with inspectable error codes/messages. The legacy flat constructor remains only as a compatibility boundary and creates a single-level table.

The Beat factory deterministically generates common harmonic mip layouts (`64, 32, 16, 8, 4, 2, 1` at the default size) outside playback. Every generated level is DC-removed, phase-aligned by common harmonic synthesis, and scaled with the frame's common full-band normalization gain. The cache publishes the completed immutable table through existing `shared_ptr<const Wavetable>` voice ownership; audio-thread playback performs no generation or allocation.

`WavetableOscillator` no longer derives frame position from pitch. Position crossfades only adjacent timbral frames. The continuously valued `0.48 * sampleRate / frequencyHz` harmonic budget selects and logarithmically crossfades adjacent mip levels; removing integer quantization avoids discontinuities at mip boundaries. Phase and preset/automation IDs are unchanged.

Focused native tests cover malformed input, finite/bounded output, frame-position invariance between low and high notes, independent mip selection, frame and mip transition continuity, deterministic generation, generated-level DC, all existing custom/warp behavior, and the five-rate preparation suite. Full `verify:non-native`, Release `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` pass with only the existing TCC waiver.

The final 150-render matrix reproduced twice with zero WAV mismatches. Compared with the canonical sample-rate freeze, all 25 initialization renders remain byte-identical; the 125 position-dependent renders change intentionally because pitch no longer clamps timbral position. JSON SHA-256: `c8da1d0a0aa34849c501d5aaa3e4d850f67b1ca76efd9444af0d57b65f3011fc`; render-manifest SHA-256: `bb299db960d7942995e38a2b7361b9b14730e3497cd5313380b0fa738f77ae4f`.

Native stress completed in 7.12 s with 189,284,352-byte maximum RSS and no new waiver. Harness render measurements were 12.78–15.92 ms with 15,073,280-byte peak RSS and zero coarse deadline overruns. These harness times include constructing a complete multi-mip table for every render, so they measure generation plus playback rather than callback cost; production tables are generated on cache-miss setup paths and reused. Composite-signal alias fields remain descriptive rather than release thresholds.

This completes only the A1 frame/mip foundation. Bounded table-replacement crossfades, destruction deferral proof, central parameter policy, unified de-clicking, deterministic stealing, callback instrumentation/budgets, DC blocking, and explicit quality modes remain open Milestone A slices.

## Milestone A2 realtime parameter policy — 2026-07-11

The realtime voice surface has one allocation-free constexpr metadata authority in `ParameterPolicy.h`. It began with 24 entries and expands to 28 with Milestone B9's Macro 5–8 IDs. Each entry preserves its stable automation ID and defines minimum/maximum range, rate class, smoothing ownership, and modulation eligibility. The taxonomy supports discrete, smoothed-control, sample-accurate-control, and audio-rate classifications; current realtime entries are classified according to their implemented processing path rather than claiming unsupported audio-rate modulation.

`VoiceRealtimeParams` now derives ID lookup and clamping from this table instead of parallel string and range switches. `InstrumentVoice` asks the policy for the effective ramp length before activating its existing sample-by-sample ramp. Every currently supported entry retains `callerRamp`, so caller-selected timing, zero-ramp immediacy, inactive-voice behavior, presets, automation IDs, and modulation semantics are unchanged.

Compile-time checks require the policy count to match the realtime enum and reject duplicate stable IDs. Native coverage verifies complete lookup round trips, unique/nonempty IDs, valid ranges, exact clamping, smoothing ownership, representative rate classes, the 15 currently eligible modulation targets, and unknown-ID rejection. The full native and non-native gates pass with only the existing TCC waiver, and all 150 WAVs are byte-identical to the A1 freeze.

This slice makes metadata authoritative for the implemented realtime voice surface. Parameters that are not currently realtime-applicable are not falsely advertised as supported; extending the surface requires adding policy metadata and coverage in the same change.

## Milestone A3 deterministic steal transitions — 2026-07-11

Beat now owns synthesiser victim selection through `BeatSynthesiser`, overriding JUCE's selection hook without modifying JUCE. Every `InstrumentVoice` exposes bounded allocation state: active/released flags, current stereo output level, and a stable construction-order voice ID. Victims are selected lexicographically: released first, then quietest, then oldest, then lowest stable ID. This ordering is allocation-free and deterministic for equal event streams.

A true victim selection explicitly arms a fixed `VoiceTransition`; ordinary hard stops do not. On immediate reuse, the new note's first stereo sample is exactly the victim's last stereo output and a linear 1.5 ms handoff (clamped to 8–256 samples) reaches the replacement signal. This avoids a steal-boundary step while preserving MIDI event timing, note phase setup, envelopes, and voice limits. No stale transition survives an unrelated hard stop.

Tests cover the complete victim comparator, real oldest and released-voice selection through a two-voice synthesiser, stable IDs, bounded transition length, exact first-sample continuity, completion, and actual one-voice stealing. Full native/non-native gates pass with only the TCC waiver, production Beat builds, and all 150 non-steal baseline WAVs remain byte-identical.

This checkpoint addresses deterministic voice stealing and steal de-clicking. General route/effect/source/table replacement transitions and explicit reclamation remain open.

## Milestone A4 durable telemetry and hard budgets — 2026-07-11

`RenderTimingSnapshot` now publishes cumulative realtime-parameter queue accepted/rejected counts, block-event overflow, deadline overruns, and callback-capacity safety violations alongside existing timing/cache/work counters. Queue admission increments atomics at the producer boundary; block-capacity drops increment a separate counter; deadline comparison uses the active sample rate and block size; callback buffer growth risk is detected before `setSize()`.

`RenderBudgets.h` centralizes fixed limits: 1024 queued realtime events, 256 drained per block, 2048 block parameter events, 1024 route events, 256 pending note-offs, 64 instrument routes, 192 sample voices, and 256 audio-clip voices. Callback admission no longer grows these containers. Excess sample/clip voices are rejected and counted; excess deferred note-offs are converted to an end-of-block note-off to avoid stuck notes; excess route/events are deterministically omitted and counted. In-budget behavior is unchanged.

Native saturation coverage proves exact queue capacity, nonzero rejection, cumulative counters, zero prepared-capacity violations, and detection when a deliberately oversized test block would require callback growth. The complete native suite passes in 6.57 s with 185,991,168-byte maximum RSS. Full non-native verification passes, and all 150 in-budget WAVs remain byte-identical to the A1 freeze.

The callback-safety counter detects known preallocation boundary violations; it is not yet a global malloc/lock/file-I/O interposer. That stronger detector and deferred shared-owner reclamation remain open.

## Milestone A5 master DC invariant — 2026-07-11

A fixed-state `MasterDcBlocker` now processes the audible master signal after the master chain and before the final limiter. It implements a 5 Hz first-order high-pass recurrence, recomputes its coefficient for the active sample rate, supports up to the engine's fixed 32-channel limit, suppresses denormals/non-finite output, and performs no callback allocation.

State persists across ordinary blocks for block-size equivalence. Explicit hard transport reset and project replacement clear it, preventing sub-audible filter decay from leaking across those lifecycle boundaries. Monitoring disable alone retains the physically correct bounded filter decay; the stress contract measures it below `1e-5` total energy rather than requiring an impossible instantaneous state disappearance.

Native tests cover 44.1/192 kHz coefficient ordering, one-second constant-signal rejection, stereo polarity, finite output, exact split/whole-block equality, monitoring decay, hard-stop silence, and project-replacement silence. Full native and non-native gates pass with only the existing TCC waiver. `BeatAetherBaseline` remains byte-identical because that isolated oscillator harness intentionally bypasses the production master chain; full-engine native render tests provide the DC-stage evidence.

## Milestone A6 explicit quality modes — 2026-07-11

`AudioQuality` defines explicit `standardLive` and `offlineHighQuality` modes. Standard Live remains the default for devices, ordinary offline engines, and all existing export entry points, so no render silently changes quality or semantics. Callers may explicitly select Offline HQ through `AudioEngine::setProcessingQuality()`.

The mode propagates to every existing and newly created `InstrumentVoice` and its legacy/A/B/unison wavetable oscillators. Offline HQ uses four-point Catmull-Rom sample interpolation inside each already selected frame/mip; frame selection, mip selection, phase delta, event offsets, parameter ramps, modulation evaluation, envelopes, and routing are identical to Standard Live.

Tests prove Standard Live remains the default and bit-stable, HQ is finite/bounded and audibly/numerically distinct on wavetable material, oscillator phase remains sample-exact between modes, and two full engines reach the exact same sequencer position. The full native suite passes with only the existing waiver. Offline HQ remains opt-in until a product-facing export-quality choice and performance gate are approved.

## Milestone A7 bounded table replacement and reclamation — 2026-07-11

`WavetableOscillator` now maintains independent current/previous immutable playback caches during replacement. A valid table change begins a 5 ms transition clamped to 32–512 samples; the first replacement sample is exactly the continuing old-table sample, then playback crossfades to the new table at the same phase, frequency, position, frame mix, and mip mix. Reapplying the same pointer is a no-op, and a newer replacement deterministically supersedes an in-flight one.

Each `InstrumentVoice` retains one retired `shared_ptr<const Wavetable>` for the legacy, A, and B stacks. Oscillators read raw immutable views, while retained owners guarantee lifetime through the transition. Ownership is bounded at current plus one retired table per stack. Production `setParams()` occurs during control-thread synth construction/replacement under the engine boundary; cache eviction and voice teardown likewise occur off the callback. A subsequent controlled replacement reclaims the prior retired owner only after its oscillator view has been superseded.

Tests cover exact first-sample continuity, bounded completion, repeated replacement during an active transition, finite output, same-pointer no-op behavior through existing note reconfiguration, and all prior frame/mip/quality invariants. Standard single-table renders remain unchanged.

This completes the planned Milestone A production foundation. A release-gate review still needs to assess the remaining global malloc/lock/file-I/O interposer limitation and product-facing Offline HQ selection before declaring the entire phase releasable.

## Milestone A8 callback heap-allocation proof — 2026-07-11

`BeatBackendStress` now links a test-only global C++ allocation interposer and enables it directly around one warmed, dense-modulation `AudioEngine::audioDeviceIOCallbackWithContext()` invocation. The probe records every ordinary, array, aligned, and nothrow `new` call without allocating in the recorder itself. It initially exposed 14 callback allocations: route-event sorting, route-automation `juce::String` slicing, a potentially growing parameter fallback, a callback-local MIDI buffer, and an owning sequencer callback wrapper.

Production corrections preserve DSP and event semantics: the callback MIDI buffer is prepared once; route events use bounded stable insertion ordering; effect IDs and parameter keys are compared as string regions without temporary strings; unknown route parameters are rejected instead of appended on the callback; and `Sequencer` accepts non-owning allocation-free callback views whose lifetime is limited to the synchronous render call. The dense callback probe now reports exactly zero heap allocations. Existing capacity/overflow telemetry remains active and no assertion was weakened.

The complete native stress executable passes with only `baseline.recent-project-exists` waived (7.26 s wall / 5.98 s user / 0.89 s system; 207,683,584-byte maximum RSS). `npm run verify:non-native` passes end-to-end, the production `Beat` and `BeatAetherBaseline` targets build, and the complete 150-WAV matrix is byte-identical to the A1/A7 freeze. The new report SHA-256 is `430bf90db6c2efd652eafa3b15408f8a51092cbe296b540ac8b83e6d47e3afd8`; the baseline run completed in 2.27 s wall / 2.17 s user / 0.06 s system with 15,613,952-byte maximum RSS, zero deadline overruns, and the unchanged queue result of 64 accepted / 16 rejected / 16 overflow.

This closes callback heap-allocation detection for the exercised dense core path. Arbitrary client-provided `onSegmentTriggered` or `onPositionChanged` callbacks remain external boundaries. OS/JUCE lock and file-operation interception is completed by the following A9 gate.

## Milestone A9 expanded realtime-safety instrumentation — 2026-07-11

The stress executable now links a development/test-only fixed-capacity realtime-safety probe. While explicitly active on the simulated device callback thread it intercepts all C++ `new` variants, `pthread_mutex_lock`, POSIX `open`/`openat`/`read`/`write`, and C `fopen`/`fread`/`fwrite`. A blocking lock violation is recorded only when a nonblocking try proves the mutex is busy; uncontended JUCE synthesiser acquisition retains its normal semantics. File and stream operations are forbidden whenever the probe is active. Every report is a thread-local write into a preallocated 128-entry array containing violation kind and return address; symbol resolution and diagnostic formatting occur outside the callback.

Compile-time hooks cover explicit lazy-initialization and container-growth boundaries. Production builds compile those hooks to no-ops. The callback's existing buffer-capacity check now emits a development-only container-growth violation in addition to durable production telemetry. Heap-backed lazy initialization is also caught by the allocator interposer, while the explicit lazy-init hook covers nonallocating initialization sites as the architecture expands.

Focused negative tests deliberately trigger and verify each category: non-elidable allocation, a mutex held by another thread, temp-file open/read, explicit lazy initialization, and explicit container growth. A prepared dense offline engine renders with the probe inactive, followed by an empty probe window, proving ordinary offline rendering is not classified as a real-time callback. The warmed dense device-callback simulation passes with zero violations across allocation, actually blocking locks, file operations, lazy initialization, and growth.

The full native suite passes with only `baseline.recent-project-exists` waived. Full non-native verification passes, production `Beat` and `BeatAetherBaseline` build, and all 150 WAVs are byte-identical to the A1/A7 freeze. Instrumentation report SHA-256 is `2fc02f1ba65191821360f5ccfcee237faa85155983fff60926930aacdb4d3935`; the matrix completed in 2.38 s wall / 2.17 s user / 0.05 s system with 15,450,112-byte maximum RSS and zero deadline overruns. No upstream code or dependency was introduced.

Per the approved review decision, the traced A8 boundaries were sufficient to complete Milestone A; A9 is the stronger prerequisite for the expanded Milestone B graph. External product callbacks remain explicitly outside Beat's internal guarantee and must themselves obey the same contract when installed.

## Milestone B1 oscillator A/B unison independence — 2026-07-11

Milestone B begins by removing the remaining shared static unison configuration from the two main oscillator contracts. New stable parameters `osc.a.unison.voices/detune/spread` and `osc.b.unison.voices/detune/spread` independently configure the already separate A/B wavetable banks and unison plans. Voice count is bounded to 1–8, detune to 0–100 cents, and spread to 0–1. Source, wavetable, tuning, phase/randomization, level, pan, and table ownership were already independent and remain unchanged.

Compatibility is explicit: patches without the six new fields inherit the existing shared `unison.voices/detune/spread` values. Existing frontend shared-unison controls write both oscillator parameter sets, while direct per-oscillator edits may diverge them. Draft normalization, preview conversion, native patch conversion, and reverse preview conversion preserve the new values; existing automation IDs are not renamed or removed.

Native contract coverage verifies A at 3 voices / 11 cents / 0.27 spread and B at 7 voices / 31 cents / 0.83 spread in one patch. Frontend coverage verifies legacy shared-value inheritance and the same divergent preview configuration. Full native stress passes with only the TCC waiver, full non-native verification passes, production Beat builds, and all 150 frozen baseline WAVs are byte-identical. Baseline JSON SHA-256 is `b6dfabee16c5b61bee82ddfb29e9a5a7ab3a6ef2cd504b502e293984b6e13b0b`.

This slice does not yet add per-oscillator FX sends, shared-filter routing, audio-rate cross-modulation, or separate unison modulation routes; those remain later B gates.

## Milestone B2 independent A/B unison modulation — 2026-07-11

Four stable modulation targets extend the B1 static contract: `osc.a.unison.detune`, `osc.a.unison.spread`, `osc.b.unison.detune`, and `osc.b.unison.spread`. Each route is evaluated only for its owning oscillator and added to the legacy shared unison route, preserving existing patches while allowing deliberate divergence. Detune remains clamped by the existing wavetable-bank plan in cents and spread by its existing bounded stereo plan.

The targets flow through frontend normalization/labels/preview, native patch conversion, activity flags, cached modulation planning, and the separate A/B renderer calls. Native tests verify distinct macro routes reach only A detune and B spread; frontend tests verify route admission and target summaries. The expanded A9 callback-safety test remains green because route state is prepared outside the callback and evaluation adds no allocation, file operation, lock contention, lazy initialization, or container growth.

Full native and non-native suites pass with only the TCC waiver, production Beat builds, and all 150 frozen WAVs remain byte-identical. B2 baseline JSON SHA-256 is `8a64d2389a2d4578e2cf6455d4a99f4b63ae647831b108791825f6c7e611ecce`.

Shared legacy targets remain supported and additive. Per-source filter/direct/FX routing and audio-rate oscillator cross-modulation remain open.

## Milestone B3 oscillator tuning modes — 2026-07-11

Oscillators A and B now independently select one of four explicit tuning contracts. `semitone` preserves the existing octave + semitone + fine-cent calculation bit-for-bit. `harmonic` multiplies the octave base by an integer harmonic 1–64; `ratio` applies a bounded positive numerator/denominator; and `step` applies an integer step in an equal division of the octave (1–96 divisions). Fine cents remain available in every mode and octave remains an independent coarse multiplier.

The persisted parameters are namespaced per oscillator under `osc.[a|b].tuning.*`. Defaults are semitone mode, harmonic 1, ratio 1/1, and step 0 of 12, so old documents and automation retain their prior result. Native and frontend conversion preserve all mode fields. Focused native tests verify semitone, harmonic, 3:2 ratio, and 7-of-19 step mathematics; frontend tests verify harmonic, ratio, and step round trips into preview state.

No lookup table, allocation, lazy initialization, or container mutation was added to the callback. Tuning selection is a cached setup-time pitch-rate calculation and remains compatible with the A9 realtime-safety gate.

Full native and non-native suites pass with only the existing TCC waiver, production Beat builds, and the 150 frozen WAVs are byte-identical to B2. B3 baseline JSON SHA-256 is `650cef36a89d84e52b360ecf78003ca910c595554ef0794b46766ee242b051bf`.

## Milestone B4 oscillator phase memory — 2026-07-12

Oscillators A and B now independently choose `retrigger` or `memory` phase behavior through stable `osc.[a|b].phaseMode` fields. Retrigger remains the default and preserves the existing explicit phase plus deterministic random-phase depth. Memory mode preserves each oscillator's phase across ordinary hard note boundaries and voice reuse instead of applying a new reset; A and B can select different modes.

Basic waveform paths now maintain separate A/B base accumulators rather than deriving both from the legacy shared phase. Wavetable stacks preserve every unison member's existing oscillator phase across setup reconfiguration in memory mode. Project replacement still creates/reset voices through the existing lifecycle, and legato continues its pre-existing no-reset path. Sample-rate preparation does not reset phase.

Native tests render both basic and wavetable sources, verify phase advances, prove exact preservation across stop/restart in memory mode, and prove retrigger mode returns the basic accumulator to zero. Native patch parsing and frontend preview/roundtrip tests cover the new mode. The implementation uses fixed stack arrays and existing oscillator state; no callback allocation, file operation, blocking wait, lazy initialization, or container growth is introduced.

Full native and non-native suites pass with only the existing TCC waiver, production Beat builds, and all 150 frozen WAVs are byte-identical to B3. B4 baseline JSON SHA-256 is `9e95a0611f7c4ca33158d6ab4fa7dd20df9da80e11cfc23ec4e26f15d5580b7f`.

## Milestone B5 dual shared-filter foundation — 2026-07-12

The voice engine now owns two prepared shared filter stages. Filter 1 is the existing filter and retains all stable parameters and modulation behavior. Filter 2 is opt-in with independent enabled, type, cutoff, resonance, and drive fields. `filter.routing` selects serial or parallel topology; serial feeds Filter 1 into Filter 2, while parallel processes the same pre-Filter-1 input through both branches and averages them for bounded gain.

Filter 2 state and oversampled drive state are prepared/reset with the voice lifecycle and never created in the callback. Disabled Filter 2 executes the original Filter-1 path without an additional arithmetic mix, preserving old renders. Native patch and frontend conversions preserve the new schema, and browser preview maintains an independent second filter state per channel.

Focused native tests verify parsing, bounded finite output, and distinct serial, parallel, and Filter-1-only renders. Frontend coverage verifies Filter 2 settings and topology reach preview state. Per-source selection between Filter 1, Filter 2, both, and direct remains the next routing slice.

The first complete non-native run exposed and retained a migration regression test: disabled Filter 2 cutoff was incorrectly sanitized from its Hz domain to `1`. The sanitizer now treats both filter cutoff IDs as 20–20,000 Hz, and the full roundtrip gate passes. Full native/non-native suites pass with only the existing TCC waiver, production Beat builds, and all 150 frozen WAVs are byte-identical to B4. B5 baseline JSON SHA-256 is `12d67613f247312e3cfdceb6bc69c1a46a5f0935e32af7869da1777153a1250a`.

## Milestone B6 per-source direct routing — 2026-07-12

Oscillator A, oscillator B, sub, and noise now independently select the stable `filter` or `direct` destination. `filter` remains the compatibility default. The stack renderer keeps one shared level normalizer, then publishes separate fixed stereo buses; the filtered bus follows the existing runtime-warp and Filter 1/Filter 2 path, while the direct bus receives an independent runtime-warp state and rejoins after both shared filters/drives but before the common amp, pan, and steal transition.

Focused native coverage proves filtered and direct buses can be nonzero simultaneously and recombine to the bounded compatibility frame. Patch parsing verifies all four route fields. Frontend normalization, reverse conversion, and preview use the same destinations; the existing default preview remains exactly RMS `0.05915262597409699`, peak `0.5288043022155762`.

Full native stress passes with only `baseline.recent-project-exists` waived, full non-native verification passes, and production `Beat` plus `BeatAetherBaseline` build. The 150-WAV matrix is byte-identical to B5; report SHA-256 is `48f94deb7d80373e3ee1ad5f9fdf162762bdad935e5f634a4289c7dc1e017d2c`, peak RSS is 15,335,424 bytes, deadline overruns are zero, and queue telemetry remains 64 accepted / 16 rejected / 16 overflow. Filter-1-only, Filter-2-only, both-filter, and FX-send destinations remain later routing slices.

## Milestone B7 explicit filter destinations — 2026-07-12

Each Aether source now accepts `both`, `filter1`, `filter2`, or `direct`; legacy `filter` is retained as an alias for `both`. `both` follows the existing global serial/parallel topology. Explicit Filter 1 and Filter 2 destinations use isolated, preprepared filter/drive/runtime-warp state lanes, preventing a shared state object from being processed twice in one sample when sources choose different destinations.

Native tests prove all four renderer buses are independently populated, bounded, and recombine correctly; voice tests prove Filter-1-only and Filter-2-only renders are finite and audibly distinct. Frontend normalization and preview maintain corresponding isolated filter-state lanes. Default preview RMS/peak remain unchanged.

Full native stress passes with only the existing TCC waiver, full non-native verification passes, and production targets build. All 150 default-route WAVs are byte-identical to B6. B7 report SHA-256 is `1f3a4ddd44fb3117a326ec939a75267e23ede01d88aefcfad65cea0084a27125`; peak RSS is 14,860,288 bytes, deadline overruns are zero, and queue telemetry remains 64 accepted / 16 rejected / 16 overflow. Per-source FX sends remain open.

## Milestone B8 dual serial runtime warp — 2026-07-12

Aether now exposes two independent serial runtime warp stages. Stage one retains `aether.runtimeWarp` and `aether.runtimeWarpMode`; stage two adds stable `aether.runtimeWarp2` and `aether.runtimeWarp2Mode` fields and defaults to zero, preserving every existing patch and render. Both stages use the existing bounded fold, pinch, mirror, and shape set and the existing two-point nonlinear evaluation/downsample state. Every source destination lane owns fixed stage-one and stage-two state, so filter/direct routing does not share nonlinear history.

Native coverage verifies patch conversion, project persistence, finite output, and a non-null audible delta between one and two enabled stages. Frontend normalization, reverse conversion, and preview apply the stages in the same order. The A9 callback probe remains at zero violations. This completes the two-stage architecture but does not invent an alias threshold: the existing oversampling stop-band/alias specification remains an explicit release-quality follow-up.

Full native and non-native suites pass with only the TCC waiver; production targets build; all 150 default-stage-two-off WAVs are byte-identical to B7. B8 report SHA-256 is `1d448e4477c529783952236a9478c40c41daa417ccff6b78cdf1bd6db42e60c4`, peak RSS is 15,089,664 bytes, deadline overruns are zero, and queue telemetry remains 64 accepted / 16 rejected / 16 overflow.

## Milestone B9 eight-macro modulation surface — 2026-07-12

The stable macro surface expands from four to eight with `macro.5` through `macro.8`. Native value storage is a fixed eight-float array; every dynamic target carries fixed Macro 5–8 route amounts; evaluation remains a bounded straight-line calculation. The authoritative real-time parameter policy now contains 28 entries and exposes all eight macros with the same sample-accurate-control/caller-ramp contract.

Project persistence accepts old four-value arrays and zero-fills the new tail, while new documents preserve all eight values and route amounts. Frontend draft normalization, macro definitions, automation targets, modulation-matrix sources, preview evaluation, and node-editor validation expose the same IDs. Existing factory guide fallback routes remain limited to Macros 1–4, so normalization does not silently alter legacy preset sound; Macros 5–8 receive neutral definitions until deliberately routed.

Focused native tests verify Macro 8 evaluation, parsing, and all eight persistence values. Frontend tests verify Macro 5–8 normalization. Full native stress and A9 instrumentation pass with only the TCC waiver; full non-native verification and production builds pass; all 150 WAVs are byte-identical to B8. B9 report SHA-256 is `56493200ccbc425d1d670063fdd5fa376168a4ec9ba07ddba68258a695c02f37`, peak RSS is 15,155,200 bytes, deadline overruns are zero, and queue telemetry remains 64 accepted / 16 rejected / 16 overflow.

## Milestone B10 four-envelope modulation foundation — 2026-07-12

Envelope 3 and Envelope 4 are now complete fixed voice modulation sources alongside the existing amp and mod envelopes. Each has stable ADSR, attack/decay/release curve, sustain, loop, bipolar-route, persistence, and preview fields. Their ADSR/loop state is prepared with the voice lifecycle; per-sample evaluation occurs only when cached route activity marks the source as needed. Envelope 1/2 behavior and timing are unchanged.

The new source values are threaded through every oscillator, unison, filter, drive, amp, and pan modulation target. Native patch and project persistence preserve envelope settings and route amounts. Frontend normalization, envelope editor summaries, modulation-matrix source selection, and browser preview support `env.3` and `env.4` with the same curve/loop semantics.

Focused tests verify Env 3/4 arithmetic, patch parsing, persistence, and frontend normalization. Full native stress including A9 passes with only the TCC waiver; full non-native verification and production builds pass; all 150 unrouted-default WAVs are byte-identical to B9. B10 report SHA-256 is `924758cfb2dcc1744c87dad5f7419717286f92cb846cfb93f035d56631d5cda8`, peak RSS is 13,189,120 bytes, deadline overruns are zero, and queue telemetry remains 64 accepted / 16 rejected / 16 overflow.

## Milestone B11 ten-LFO modulation foundation — 2026-07-12

The modulation source surface now contains ten LFO slots. LFO 1/2 retain their legacy scalar fields and behavior. LFO 3–10 use fixed eight-slot configuration, phase, activity, raw-value, route-amount, and bipolar arrays, avoiding dynamic graph ownership and repetitive callback containers. Each extra slot supports enabled, waveform, rate, smoothing, random phase, phase offset, retrigger, and one-shot semantics.

Cached target activity and per-slot enablement jointly gate evaluation and phase advancement, so disabled or unrouted extra slots remain zero-work. Extra raw values are threaded through every oscillator, unison, filter, drive, amp, and pan target. Patch/project persistence and browser preview preserve the same stable `lfo.3` through `lfo.10` IDs. LFO 1/2 synchronization behavior is unchanged; tempo-sync fields for LFO 3–10 are intentionally not claimed in this foundation.

Focused coverage verifies LFO 10 arithmetic, parsing, persistence, rate sanitization, and frontend normalization. Full native stress including A9 passes with only the TCC waiver; full non-native verification and production builds pass; all 150 default-extra-LFO-disabled WAVs are byte-identical to B10. B11 report SHA-256 is `9f53f8ad5d6e3ab89d0dcfb0174c8f12c5db7d59b36b134b5ce94c63f9966bf9`, peak RSS is 14,925,824 bytes, deadline overruns are zero, and queue telemetry remains 64 accepted / 16 rejected / 16 overflow.

## Milestone B12 callback work ceilings — 2026-07-12

The fixed voice graph now has explicit structural callback ceilings of 16 modulation evaluations and 32 oversampled nonlinear evaluations per rendered voice sample. The modulation ceiling covers voice automation, the legacy LFO path, and all eight fixed extra-LFO slots with headroom for fixed graph bookkeeping. The nonlinear ceiling exactly covers four destination lanes through two serial warp stages at four evaluations per stereo lane. Saturating ceiling arithmetic prevents telemetry overflow. These are invariant limits: normal audio is never conditionally truncated or quality-degraded.

Voice telemetry now counts each active extra LFO and every executed runtime-warp lane. At the audio-engine block publication boundary, allocation-free atomic counters record modulation or nonlinear work that exceeds its structural ceiling. Native-to-frontend timing IPC exposes voice nonlinear work and cumulative overrun counts; the development timing panel displays them. Focused negative tests prove equality passes, one-unit excess fails, negative work fails, and multiplication saturates safely. The complete A9 callback-safety gate remains green and offline rendering remains outside callback instrumentation.

Full native stress passes in 8.23 s with only `baseline.recent-project-exists` waived. Full non-native verification passes. All 150 WAVs are byte-identical to B11. B12 baseline JSON SHA-256 is `ee404aa66fb3f93beb05d963fe3c44512fbc7723e65c29fb8ac4035f70ed56db`; the isolated matrix completed in 2.25 s wall / 2.16 s user / 0.06 s system with 15,286,272-byte peak RSS, zero deadline overruns, and 64 accepted / 16 rejected / 16 overflow queue events. No upstream code, dependency, or production DSP output change was introduced.

## Milestone B13 bounded A/B interaction — 2026-07-12

Oscillator A can now opt into two deliberately limited interactions with oscillator B: amplitude modulation and ring modulation. Amount zero/off remains the exact compatibility path. The interaction crossfades only oscillator A's existing contribution toward the interacted carrier, retains A's level/pan/destination, leaves B independently audible/routable, clamps the public amount to 0–1, rejects non-finite products, and adds no container or ownership work. The fixed nonlinear ceiling expands from 32 to 33 evaluations per voice sample: 32 for both warp stages across four lanes plus one A/B multiplication.

The current oversampling policy is explicit: A/B multiplication is not oversampled. A test-only 4096-point Hann/FFT fixture at 44.1 kHz deliberately drives a 7040 Hz carrier and third-harmonic modulator, whose 28160 Hz upper sideband folds to 15940 Hz. Measured alias-to-total energy is `0.0705697` for AM and `0.24735` for ring. These are captured baselines, not release-quality thresholds; the material ring-alias result keeps oversampling/stop-band design open before B release. Both modes remain finite across 44.1, 48, 88.2, 96, and 192 kHz.

Native patch conversion, project persistence, work accounting, mode-difference, finite/bounded output, five-rate, and spectral tests pass. Frontend normalization, reverse conversion, deterministic preview, and roundtrip tests pass. Full native stress passes in 8.51 s with only the TCC waiver; full non-native verification and production builds pass. All 150 default-off WAVs are byte-identical to B12. B13 baseline JSON SHA-256 is `c5cd84596e44b8638a2cd6d392fdc8b5e767abb203f0270c2038273f76a29b96`; the matrix completed in 2.26 s wall / 2.18 s user / 0.06 s system with 15,319,040-byte peak RSS, zero deadline overruns, and unchanged queue telemetry.

## Milestone B14 independent pressure and timbre sources — 2026-07-12

The fixed modulation matrix now exposes `pressure` and `timbre` as independent performance sources rather than mapping aftertouch to mod wheel. JUCE polyphonic aftertouch and channel pressure update the voice-owned normalized pressure value; MIDI CC74 updates the separate normalized timbre value; CC1 retains its existing mod-wheel value. Every dynamic target stores fixed pressure/timbre amounts and bipolar flags, and the values are evaluated through the existing bounded straight-line target calculation. The change does not add callback containers, allocation, ownership changes, lazy initialization, filesystem access, or locks.

Patch conversion, project persistence, native expression telemetry, IPC, browser MIDI parsing, modulation-matrix selection, editor activity summaries, factory-guide normalization, and deterministic preview all preserve the distinct source IDs. Native focused coverage verifies exact arithmetic/activity flags, channel and poly pressure, CC74, state clearing, patch conversion, repository roundtrip, and audible voice response. Frontend coverage verifies parsing/tracking, separate live status, normalization, persistence roundtrip, and that pressure/timbre preview offsets do not alias mod wheel or each other. This is a channel/poly-pressure and CC74 foundation, not a claim of complete MPE member-channel ownership or per-note pressure persistence.

Full native stress passes with only `baseline.recent-project-exists` waived; the unwaived run still exposes that assertion before the explicit waiver is applied. Full `verify:non-native` and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. All 150 default-unrouted WAVs are byte-identical to B13. B14 baseline JSON SHA-256 is `ea2f4e30eb4b2c4a802c542bcae531d1fc19e6a56fa3873a85ac5c66df8a5e06`; the normalized 150-file hash manifest is `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`. The matrix completed in 2.47 s wall / 2.16 s user / 0.07 s system with 15,466,496-byte process maximum RSS (14,958,592-byte harness-reported peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. No upstream code or dependency was introduced.

## Milestone B15 LFO 3–10 tempo sync — 2026-07-12

LFO slots 3–10 now support the same explicit free-rate versus tempo-synced rate contract as LFO 1/2. Each fixed extra-LFO configuration persists `sync` and `syncedRate`; the compatibility default is sync off with the existing 1 Hz free rate. On project application, Beat converts the selected musical division to a bounded cycles-per-second value using the current sequencer tempo before publishing fixed voice parameters. The audio callback still advances each enabled/routed slot from one cached phase delta and performs no string parsing, division lookup, allocation, lock, file operation, lazy initialization, or container growth.

The shared native rate helper preserves quarter-note, dotted, and triplet mathematics and returns the exact stored free rate when sync is disabled. Native tests cover 60/120 BPM quarter-note rates, an eighth-note triplet, free-rate BPM invariance, patch parsing, project persistence, and a full `AudioEngine` render whose routed LFO 10 response changes with tempo. Frontend normalization, editor controls, labels, persistence roundtrip, and preview calculations use the same stable `lfo.3.sync/syncedRate` through `lfo.10.sync/syncedRate` IDs and verify both synced and free-rate behavior.

Full native stress passes with only the existing TCC waiver; full non-native verification and production builds pass. All 150 default-extra-LFO-disabled WAVs are byte-identical to B14. B15 baseline JSON SHA-256 is `6afccadd90b3db75663a89368bcc8d132289e68ebf8ab6408381297d444d23de`; the normalized render manifest remains `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`. The matrix completed in 2.38 s wall / 2.17 s user / 0.07 s system with 15,859,712-byte process maximum RSS (15,384,576-byte harness-reported peak), zero deadline overruns, and unchanged queue telemetry. No upstream code or dependency was introduced.

## Milestone B16 fixed per-source FX buses — 2026-07-12

Aether now exposes two fixed source-send levels for each of oscillator A, oscillator B, sub, and noise. Each bus targets an explicit project return-bus ID, so effects remain shared route-owned processors rather than per-voice instances. The tap is the normalized source contribution after source level/pan and A/B interaction, before the shared filter/warp lanes; the voice envelope, velocity, amp level/pan, and an independent bounded steal transition are then applied. AudioEngine accumulates voice output into two route-owned, setup-preallocated stereo buffers, applies route gain/pan, and adds each buffer to its selected return before the existing return effect chain. Main stereo synthesis, instrument inserts, ordinary track sends, groups, and returns retain their existing ordering.

`AetherSourceBusContext` is a render-scope, non-owning thread-local pointer pair. It performs only bounded sample adds while JUCE synchronously renders the route synth; buffer ownership, sizing, clearing, and destruction remain in `AudioEngine`. Missing, muted, or deleted targets discard the auxiliary signal. Default send levels and target IDs are zero/empty, making existing documents and renders exact compatibility paths. Persistence, IPC, synth patch conversion, frontend draft normalization, oscillator controls, sub/noise controls, and project return selectors preserve the stable two-bus contract.

Focused native coverage proves an oscillator-A-only send creates a finite audible return delta and that a muted target is exactly dry; repository roundtrip coverage preserves all eight source levels and both target IDs. The warmed dense callback allocation/lock/I/O/lazy-init/growth gate remains green. Full native stress passes with only `baseline.recent-project-exists` waived, full non-native verification passes, and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` build. All 150 default-off WAVs are byte-identical to B15. B16 baseline JSON SHA-256 is `1818c21ce8f655297e682158050c2ff9a4688df6951e3f52b8065e1ee80f8923`; a filename-normalized local render manifest is `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1` for both B15 and B16. The matrix completed in 2.29 s wall / 2.19 s user / 0.05 s system with 15,794,176-byte process maximum RSS (15,368,192-byte harness-reported peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. No upstream code or dependency was introduced.

## Milestone B17 bounded effect-graph transitions — 2026-07-12

Instrument, group, and return routes now apply the existing 1.5 ms bounded stereo transition when a same-project apply changes effect order, bypass state, kind, identity, latency declaration, or ordered parameter values. Graph equivalence and transition arming happen on the project-application path while prepared route states are rebuilt. The audio path performs only fixed transition arithmetic and captures the final stereo route sample for a possible later bridge; it allocates no memory, grows no container, opens no file, initializes no processor, and introduces no new callback lock. A different project ID retains the established full-project reset behavior, while same-project effect edits no longer reset the master DC blocker.

Focused native coverage renders a sustained note through lowpass and saturator effects, then reorders the chain and bypasses the saturator under the same project ID. Output remains finite and audible, and the measured boundary step is `0.000371158`, below the explicit `0.2` regression ceiling. Existing schema-0 migration coverage for instrument, track, and return effects remains green; B17 adds no persisted field and therefore requires no schema version change. The bounded bridge intentionally starts from the last old graph output and does not keep the old processor graph alive to render its decay tail in parallel. Broader source/table/preset replacement fixtures and a product-ratified transition ceiling remain open.

Full native stress passes with only `baseline.recent-project-exists` waived, full non-native verification passes, and production `Beat` and `BeatAetherBaseline` builds pass. All 150 WAVs are byte-identical to B16. B17 baseline JSON SHA-256 is `c6b3ab19034bab5ee43222b4a869ab858f459fb31cdcbf938440183bc3b4692c`; the filename-normalized render manifest remains `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`. The matrix completed in 2.50 s wall / 2.18 s user / 0.06 s system with 15,974,400-byte process maximum RSS (15,581,184-byte harness-reported peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. Timing and RSS deltas are descriptive single-run measurements, not regressions. No upstream code or dependency was introduced.

## Milestone B18 deterministic member-channel expression state — 2026-07-12

`BeatSynthesiser` now retains channel pressure and MIDI CC74 timbre in fixed 16-channel atomic arrays, complementing JUCE's existing per-channel pitch-wheel retention. When a note starts, its `InstrumentVoice` receives the retained state for that exact channel before any audio is rendered. Active channel-pressure and CC74 messages continue through JUCE's channel-filtered voice dispatch, while polyphonic aftertouch remains filtered by both channel and note. A voice stolen from one member channel therefore begins with the destination channel's pressure, timbre, and pitch state rather than stale values from its previous owner. Unused channels start at zero, and rebuilding a project constructs a fresh synthesiser with zeroed caches.

The caches are fixed-size and use relaxed atomic loads/stores; they allocate no memory, grow no container, perform no I/O, and add no controller-path lock. Focused native coverage proves pre-note retention, independent state on channels 2 and 3, isolation of subsequent pressure/CC74 updates, note-specific poly-aftertouch, deterministic state after a cross-channel voice steal, and zero state on a previously unused channel. The complete warmed callback-safety gate remains green. This is a deterministic Aether member-channel foundation, not a claim of complete MPE: zone/master-channel negotiation, RPN pitch-range negotiation, capability exchange, persisted performance mapping, and pre-note expression initialization for non-Aether Nodemap voices remain open.

Full native stress passes with only `baseline.recent-project-exists` waived, full non-native verification passes, and production targets build. All 150 WAVs are byte-identical to B17. B18 baseline JSON SHA-256 is `2acb16e85a8845d7d91cc8a0033bdecd95e9f8e8db21181f1bcc1ad2ce8f21b1`; the filename-normalized render manifest remains `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`. The resource-accounting matrix completed in 2.29 s wall / 2.17 s user / 0.09 s system with 15,646,720-byte process maximum RSS (15,089,664-byte harness-reported peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. No upstream code or dependency was introduced.

## Milestone B19 per-channel RPN pitch-bend ranges — 2026-07-12

`BeatSynthesiser` now recognizes MIDI Registered Parameter Number 0,0 and retains an independent pitch-bend range for each of the 16 channels. CC101/100 select the RPN, Data Entry MSB sets 0–96 semitones, and Data Entry LSB sets 0–99 cents. RPN null 127,127 deselects the parameter so later unrelated Data Entry messages cannot alter the range. A configured range is applied to a newly started or stolen Aether voice on its destination channel, and a live range change immediately recomputes that channel's active voices from their current wheel value. Channels without an RPN range continue to use the existing saved patch range and its original 0–24-semitone clamp.

All RPN selectors and values use fixed 16-channel atomic arrays. Controller ingestion allocates no memory, grows no container, performs no I/O or lazy initialization, and the bounded active-voice update uses the synthesiser's existing critical section. Focused tests cover a pre-note 48.25-semitone range, the unchanged 2-semitone fallback on another channel, live change to 12.25 semitones, exact current-wheel recomputation, RPN-null isolation, and clearing a prior negotiated override when a voice is stolen onto an unconfigured channel. This adds standards-based member-channel bend-range handling but still does not claim complete MPE zone/master-channel negotiation, automatic capability exchange, persisted controller profiles, or non-Aether pre-note initialization.

Full native stress passes with only `baseline.recent-project-exists` waived, full non-native verification passes, and production targets build. All 150 WAVs are byte-identical to B18. B19 baseline JSON SHA-256 is `b6a22a374838dd91bd893d1f407ee7ff489fc9b8ca9cf7dbbeb17b7962662700`; the filename-normalized render manifest remains `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`. The matrix completed in 2.31 s wall / 2.19 s user / 0.09 s system with 15,351,808-byte process maximum RSS (14,893,056-byte harness-reported peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. No upstream code or dependency was introduced.

## Milestone B20 explicit member-expression zones — 2026-07-13

Aether patches can now opt into one explicit contiguous member-expression zone using stable `aether.mpe.enabled`, `masterChannel`, `firstMemberChannel`, and `lastMemberChannel` fields. Default-off preserves ordinary MIDI and every existing preset. When enabled, master-channel mod wheel, CC74 timbre, channel pressure, and pitch wheel are copied to the configured member channels; member-channel messages remain isolated and can override one member without changing its siblings. Master pitch state is retained for later member note-ons, while channels outside the configured range receive no broadcast. Pre-note mod-wheel state now uses the same fixed per-channel ownership contract as pressure and timbre.

The persisted `memberExpressionZone` object has schema version 1. Missing/legacy objects migrate to the default-off contract, reversed member bounds are normalized, an overlapping master/member range is disabled, and unknown future sub-schema versions are loaded disabled. Native, backend patch-contract, repository, frontend draft/preview, and synth roundtrip tests cover the valid lower-zone contract, global master propagation, member override isolation, outside-zone isolation, invalid-zone rejection, schema preservation, and mixed-era defaults. The runtime performs only bounded loops across at most 15 channels and fixed atomic state; it adds no callback allocation, growth, file access, lazy initialization, or new ownership work.

This is explicit saved zone configuration, not automatic MPE capability negotiation. MIDI-CI/profile discovery, MIDI RPN 6 zone negotiation, product-facing configuration controls, and pre-note expression initialization for non-Aether Nodemap voices remain open. Full native stress passes with only `baseline.recent-project-exists` waived, full non-native verification passes, and production targets build. All 150 WAVs are byte-identical to B19. B20 baseline JSON SHA-256 is `b4f3a35b052d77b8bbb97c9392b6b01fdf6b7a6817ddcb6e7d3d1bf654c87d69`; the filename-normalized render manifest remains `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`. The final matrix completed in 2.26 s wall / 2.17 s user / 0.06 s system with 15,466,496-byte process maximum RSS (15,056,896-byte harness-reported peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. No upstream code or dependency was introduced.

## Milestone B21 legacy RPN 6 zone negotiation — 2026-07-13

`BeatSynthesiser` now recognizes the MIDI 1.0 MPE Configuration Message selected as RPN 0,6. Data Entry MSB values 1–15 on manager channel 1 create the lower zone over channels 2 through `1 + memberCount`; the same values on manager channel 16 create the upper zone over `16 - memberCount` through 15. A zero value clears only a currently active zone owned by that manager, so a controller's conventional clear-lower/clear-upper sequence cannot erase the other manager's active zone. Counts above 15, manager channels other than 1/16, RPN-null-selected Data Entry, and Data Entry LSB do not change the zone. A valid runtime message replaces the prepared saved zone only for the current synthesiser lifetime; it does not rewrite the project or preset.

Focused native coverage verifies exact lower and upper bounds, master CC74 propagation to both inclusive member edges, outside-channel isolation, manager-specific clear behavior, invalid-manager/count rejection, and RPN-null isolation. The controller path reuses the fixed 16-channel selector arrays and a fixed zone value; propagation remains bounded to at most 15 channel numbers. It allocates no memory, grows no container, performs no filesystem access or lazy initialization, and introduces no new lock. The complete warmed callback-safety detector remains green.

This is legacy MPE 1.1 RPN 6 reception, not MIDI-CI MPE Profile negotiation. Beat still represents one active contiguous expression zone per synthesiser, does not automatically apply the MPE Configuration Message's conventional manager/member pitch-range defaults, has no product-facing zone controls, and does not initialize non-Aether Nodemap voices from this channel state; those limitations prevent a complete MPE claim. The MIDI Association's public RPN table and MPE material were used as protocol references, and the already-pinned JUCE 8.0.4 dependency was inspected to confirm lower/upper byte mapping; no external implementation was copied or adapted.

The unwaived native run still fails only `baseline.recent-project-exists`, proving the waiver remains visible. With `AETHER_BASELINE_WAIVE_RECENT_PROJECT_EXISTS=1`, every native section passed in the final run in 8.73 s wall / 7.17 s user / 0.82 s system with 216,645,632-byte maximum RSS. Full non-native verification and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. All 150 WAVs are byte-identical to B20. B21 baseline JSON SHA-256 is `cdf14eeb7801f4ad44d7e6a689efbacb7b182ff2ebc722aa73a58f9d6566a1fa`; the filename-normalized render manifest remains `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`. The matrix completed in 2.29 s wall / 2.18 s user / 0.08 s system with 15,613,952-byte process maximum RSS (15,155,200-byte harness-reported peak), 12.77–18.34 ms measured render times, zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. No upstream code or dependency was introduced.

## Milestone B22 independent manager/member pitch bend — 2026-07-13

Legacy MPE Configuration Messages now initialize the manager channel to a 2-semitone pitch range and every negotiated member channel to 48 semitones. `InstrumentVoice` retains the member wheel and the manager/global wheel as independent fixed scalars and adds their semitone offsets immediately before pitch conversion. A manager wheel therefore no longer overwrites JUCE's retained member wheel or destroys a per-note bend. Manager RPN 0,0 changes update only the global contribution across active members; member RPN 0,0 changes remain channel-local. New and stolen member notes receive both retained values before rendering, and disabling or replacing a zone clears the retired manager contribution without clearing the member wheel/range.

Beat's pitch-wheel override now records the clamped wheel in JUCE's inherited fixed 16-channel cache before dispatch. JUCE's normal MIDI-event path already performs this write before calling the virtual override; doing it in the override as well makes direct engine/test calls and event-driven calls behaviorally identical and idempotent. Focused tests prove 48+2-semitone additive motion, sibling isolation, a note started after manager motion, live manager-range change to 12.25 semitones, zone-clear behavior, runtime replacement of a saved zone, and zero allocation/lock-blocking/file/lazy-init/growth violations around the RPN 6 path itself.

The implementation adds one voice scalar and bounded prepared-voice/channel scans; it introduces no dynamic ownership, container growth, file operation, lazy initialization, dependency, or new lock class. MIDI-CI MPE Profile negotiation, simultaneous lower+upper zones, product controls, and non-Aether expression initialization remain open, so Beat still does not claim complete MPE support.

Full native stress passed in the final run in 8.71 s wall / 7.22 s user / 0.87 s system with 193,708,032-byte maximum RSS and only `baseline.recent-project-exists` waived. Full non-native verification and production targets pass. All 150 default/non-MPE WAVs are byte-identical to B21. B22 baseline JSON SHA-256 is `b4816e0bb1839efc7b71400df338ae479a5dbd3151c03b535f74b07a813ce193`; the filename-normalized render manifest remains `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`. The matrix completed in 2.55 s wall / 2.45 s user / 0.07 s system with 15,613,952-byte process maximum RSS (15,138,816-byte harness-reported peak), 13.43–18.55 ms measured render times, zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. No upstream code or dependency was introduced.

## Milestone B23 product-facing MPE zone controls — 2026-07-13

The Aether identity panel now exposes the persisted member-expression zone directly: one default-off switch plus integer manager, first-member, and last-member channel fields. The first/last inputs constrain one another to preserve ordered bounds. A manager edit that enters the member range immediately disables the saved zone, and the switch refuses to enable an overlapping configuration. The compact status line distinguishes an enabled mapping, a valid saved-but-off mapping that may still be overridden by runtime RPN 6, and an invalid overlap. No new parameter ID or schema was introduced; the controls edit the existing B20 stable fields.

TypeScript, synth roundtrip, and the full non-native verifier pass. A source-contract check requires the labeled switch, all three accessible channel fields, and overlap-disable path. Live in-app browser verification used the `aether-performance` fixture at 1280×720: the row rendered at 812×33 px without covering the expression summary, the switch toggled on, and changing the manager into the 2–16 member range toggled the saved zone back off. Production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` build, and every native stress section passes with only `baseline.recent-project-exists` explicitly waived. This frontend-only slice does not enter the native/audio callback and cannot change render hashes; the B22 native and 150-WAV freeze remain authoritative.

MIDI-CI profile negotiation, simultaneous lower+upper zones, and non-Aether expression initialization remain open. No upstream code, runtime dependency, asset, preset, service, branding, or licensing change was introduced.

## Milestone B24 product-facing oscillator tuning and phase controls — 2026-07-13

Each oscillator row now exposes its already-persisted tuning mode and phase lifecycle fields without adding parameters or changing DSP. Semitone mode keeps the existing octave/semitone/fine controls authoritative; harmonic mode exposes the bounded 1–64 harmonic number; ratio mode exposes the existing 0.001–64 numerator and denominator; equal-division mode exposes the existing -96–96 step and 1–96 divisions; and phase mode selects the existing retrigger or memory contract independently for oscillators A and B.

Focused TypeScript and frontend-interaction checks pass. The source-contract gate requires the tuning selector, phase selector, and every mode-specific stable field. Live in-app browser verification used the `aether-performance` fixture at 1280×720: all four A-oscillator modes revealed the intended controls, Memory became the selected phase mode, and the 921×47 px advanced row remained inside the oscillator card without overlap. Full non-native verification, production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass; every native stress section passes with only `baseline.recent-project-exists` explicitly waived. This slice only edits frontend presentation of B3/B4 fields and therefore does not enter the audio callback, change persistence, or invalidate the B22 150-WAV freeze.

No upstream code, runtime dependency, asset, preset, service, branding, or licensing change was introduced.

## Milestone B25 phase lifecycle regression expansion — 2026-07-13

The test-only phase-memory fixture now covers two previously uncombined lifecycle boundaries. A one-voice `BeatSynthesiser` proves a memory-mode basic oscillator retains its exact phase across deterministic stealing while the existing bounded steal bridge preserves first-sample continuity and finite audible output. A direct legato-retune fixture proves an active voice retains its exact running phase even when the oscillator's normal non-legato lifecycle is retrigger mode; the pre-existing non-legato retrigger check still proves a fresh note resets phase. Wavetable memory, hard-stop/restart memory, and basic non-legato retrigger coverage remain asserted in the same fixture.

This slice changes only `BackendStress.cpp`; production DSP, schemas, frontend contracts, render paths, and default output are unchanged. The complete native stress executable passes with only `baseline.recent-project-exists` explicitly waived. The B24 full non-native result and B22 150-WAV freeze remain authoritative because no corresponding source entered those paths. No upstream code or dependency was introduced.

## B25 independent retest and Milestone B26 interaction-oversampling gate — 2026-07-13

An independent clean-branch retest at `74cd3c86214655323196d04d1730fdae043b07ff` passed the complete non-native verifier, production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds, and every native stress section with only `baseline.recent-project-exists` explicitly waived. The regenerated baseline contains exactly 150 WAVs and its normalized manifest is `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`, byte-identical to B22–B25. The timing-bearing JSON SHA-256 is `74002e0df95ba104cd0e13bf87b0efaec990900fcd29bc6cecde9fec99a3cf7f`; the matrix completed in 2.40 s wall / 2.17 s user / 0.05 s system with 15,712,256-byte process maximum RSS (15,220,736-byte harness-reported peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry.

B26 adds a disconnected, fixed-size interaction-oversampling candidate and analytic measurement gate; it is not called by production rendering. The candidate renders source values at 2x, performs AM/ring multiplication at that rate, and downsamples through three fixed Butterworth low-pass sections prepared outside the callback. Against the existing 44.1 kHz / 7040 Hz carrier / 21120 Hz modulator case, AM alias falls from `0.0705697` to `0.000027185`, and ring alias falls from `0.24735` to `0.000138596`. Both are below the explicit test threshold of `0.005` and more than 10x below their captured 1x baselines. Standard Live and Offline HQ currently select the same proven 2x candidate; no unsupported claim is made that 4x is automatically better.

The test demonstrates the key architectural requirement: interpolation of already-rendered base-rate sources is insufficient, so production integration must render both A and B at the oversampled rate before multiplication. The stage owns only scalar biquad history and coefficients, performs no allocation, lock, file operation, lazy initialization, or container growth in `process`, and passes the focused callback-safety probe. At the B26 gate it was still disconnected and production output remained unchanged; B27 below records the reviewed connection.

## Milestone B27 production interaction oversampling — 2026-07-13

The measured B26 stage is now connected only when A-by-B AM or ring modulation is active with a nonzero amount. Each enabled A/B source is rendered from prepared duplicate state at twice the active engine sample rate, multiplication occurs at that source rate, and the result is returned through the fixed three-section low-pass stage. Wavetable position, frequency, tuning, unison detune/spread, routing, level, and pan follow the main source state; noise uses deterministic per-source fixed PRNG state derived at note start. Note start synchronizes the duplicate banks to the main A/B phase lifecycle; ordinary parameter reapplication does not reset filter history. Prepare and quality changes recompute all sample-rate-dependent oscillator and filter state outside rendering. Standard Live and Offline HQ deliberately use the same measured 2x interaction policy; no unsupported 4x quality claim is made.

At 44.1 kHz with the captured 7040 Hz carrier / 21120 Hz modulator case, production AM alias-to-total energy is `0.0000102953` versus the historical 1x `0.0705697`, and production ring alias is `0.0000448392` versus `0.24735`. Offline-HQ ring measures the same `0.0000448392`. All are finite, below the ratified `0.005` gate, and more than 6800x/5500x below the respective historical cases. Focused tests also cover all five supported sample rates, exact interaction-off versus amount-zero output, warmed production wavetable-unison/noise callback interception, finite noise interaction output, and exact work accounting. That expansion exposed and fixed an existing B13 handoff omission where audible A/B noise returned before being marked as an interaction source; interaction-off noise is unchanged. The callback probe reports zero allocation, blocking-lock, filesystem/stream, lazy-initialization, or container-growth violations.

Interaction work now records two nonlinear evaluations per active voice sample. Basic A/B sources each report one main-rate plus two source-rate samples; wavetable sources report their actual bounded unison voice counts at both rates. The fixed nonlinear ceiling therefore changes from 33 to 34 evaluations per voice sample: 32 serial-warp evaluations plus two interaction multiplications. No audio is truncated or quality-degraded when telemetry is collected.

The complete native stress suite passes with only `baseline.recent-project-exists` explicitly waived, the complete non-native verifier passes, and production `Beat` plus `BeatAetherBaseline` build. The final regenerated matrix contains 150 WAVs and is byte-for-byte identical to the B25 independent retest because every frozen scenario leaves interaction disabled. Its timing-bearing JSON SHA-256 is `c0034c431b6fc5741d181071ab11f4b39a5f320e2dac3d033f360508a57f99c0`; the established filename-normalized manifest therefore remains `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`. The matrix completed in 2.27 s wall / 2.17 s user / 0.07 s system with 15,433,728-byte process maximum RSS (14,974,976-byte harness peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. Relative to B25 this single run is -0.13 s wall and -278,528 bytes process RSS; the delta is descriptive, not a performance conclusion. Existing interaction-enabled output changes intentionally to the measured antialiased result. No upstream code or dependency was introduced.

## Milestone B28 product-facing Offline HQ export — 2026-07-13

Export presets now carry an optional `quality` field with the stable values `standard` and `high`. Missing, malformed, and all existing factory/user preset values normalize to `standard`, preserving the prior export contract. The Export Review render settings expose Standard and Offline HQ directly at all five audited sample rates through 192 kHz, and project, range, selected-track, all-stems, and bounce render requests carry the normalized choice through both synchronous and asynchronous IPC. The native parser rejects unknown values to Standard. Offline engines apply the selected quality after preparation and before project application, so existing voices and later route-owned synths receive the same setup-time mode; live/device playback is never changed by an export choice.

The static render APIs retain Standard as their default final argument. Focused native coverage proves an omitted quality and explicit Standard produce bit-identical 32-bit WAV samples, Offline HQ is finite and measurably distinct on the same Aether wavetable project, and the existing direct-engine phase/transport parity remains green. Frontend coverage proves invalid legacy values normalize to Standard, High survives custom-preset save/edit normalization, and the product control remains wired. No project/preset automation schema or timing semantics changed.

The complete native suite passes with only `baseline.recent-project-exists` waived, full non-native verification passes, and production targets build. The Standard/default 150-render matrix is byte-for-byte identical to B27 and all earlier frozen output. B28 JSON SHA-256 is `2e6444b4fc2d4965547554e07117541afa01d0fb61a0b99b0026ff80c88b52fc`; the established normalized render manifest remains `f3d8c2fce88a0607d4f3c3043fef273617f999d9c38f1b1147327fbf73b8bba1`. The matrix completed in 2.50 s wall / 2.18 s user / 0.04 s system with 15,482,880-byte process maximum RSS (14,958,592-byte harness peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. No upstream code or dependency was introduced.

## Milestone B release review — 2026-07-13

The implementation and automated gates are green, but Milestone B is not declared released and Milestone C remains blocked. `milestone-b-release-report.md` maps every phase criterion to executable evidence and presents two explicit approval items: the bounded transition/no-old-tail-overlap policy and the mode-specific spectral-threshold policy. No new DSP, schema, dependency, or external-code state is introduced by this review checkpoint.

## Milestone B29 benchmark factory strings — 2026-07-13

The factory library now includes Benchmark - Future Bass Strings and Benchmark - Progressive House Strings, with stable IDs factory.benchmark-future-bass-strings and factory.benchmark-progressive-house-strings. Both are Beat-native structural adaptations of the user-supplied schema 2.0 documents recorded in the external-source ledger. They retain supported oscillator, unison, envelope, LFO, filter, macro, performance, modulation, and instrument-FX intent. The Future Bass adaptation uses the supported eight-voice oscillator-unison ceiling instead of the source's nine; unsupported dynamic FX/envelope/LFO-control routes are explicitly recorded rather than silently claimed.

The standalone factory-preset audit later exposed that both benchmark adaptations still used its prohibited generic `Motion / Color / Shape / Space` display layout. Their stable macro IDs, values, and routes are unchanged, but product labels now preserve the authored intent more specifically: `Pump Motion / Harmonic Color / Envelope Shape / Stereo Space` and `Pulse Motion / String Color / Envelope Shape / Hall Space`. Audition guidance and synth-roundtrip assertions use the same labels. This is metadata correction only; it changes no synthesis parameter, modulation amount, effect, preset ID, or DSP path.

verify-synth asserts exact IDs and names, Strings taxonomy, benchmark tags, core oscillator/filter values, complete effect-kind ordering, normalization idempotence, finite output, and audible RMS/peak for every factory preset. The complete non-native suite passes, including typecheck, DAW/document/interaction verifiers, and production build. That run also exposed two pre-existing Solid setter overload errors originating at aea04342; wrapping the selected File and recorded Blob as setter values resolves current TypeScript without changing runtime behavior. This slice changes frontend factory data only and adds no native/audio callback edge, DSP behavior, stable parameter ID, runtime dependency, or render-default change. The B28 native suite and 150-render freeze therefore remain authoritative.

## Milestone B release acceptance — 2026-07-13

The user explicitly approved both release-review policies: bounded transition bridges without old effect-tail overlap, and mode-specific isolated spectral gates rather than a misleading universal broad-band alias threshold. `milestone-b-release-report.md` is marked accepted at `b463dbe9`. Milestone C is unblocked for independently gated, dependency-free Beat-owned work; distribution, upstream execution/import, and new external dependencies remain separate hard approval gates.

## Milestone C1 source-slot and sample-playback foundation — 2026-07-13

Beat now has a narrow internal three-slot contract in `SourceSlot.h` and `SourceSlotRack.h`. Each slot exposes setup-only preparation and attachment, fixed-work note/render boundaries, lifecycle state, complexity class, latency, active-voice count, state version, and bounded telemetry. The rack is deliberately non-owning so heavyweight destruction cannot occur implicitly on the callback; its caller must retain slot lifetime and perform attachment/replacement outside rendering.

`SampleSourceSlot` is the first dependency-free implementation. It consumes an already-decoded immutable sample published off the callback, prepares a 128-note pitch-rate table outside rendering, and renders at most 16 fixed voices with linear interpolation, equal-power pan, a bounded 4 ms note-off release, and a 64-sample source-end fade. Capacity saturation rejects the seventeenth voice and increments telemetry rather than growing a container or degrading quality. It reports zero latency and `singleSample` complexity. Sample publication while voices are active is rejected, preventing callback-time lifetime destruction.

The focused native fixture proves all three slots attach and sum audibly, 440 Hz remains within 0.5 Hz at 48 and 44.1 kHz, note-off reaches the ready state, active publication is rejected, 16 events are accepted and the seventeenth rejected, output is finite/audible, and the warmed note/render/rack paths produce zero allocation, blocking-lock, filesystem/stream, lazy-initialization, or container-growth violations. Production Beat and BeatBackendStress build, and the complete native suite passes with only `baseline.recent-project-exists` waived. This is an internal foundation, not yet a project/preset/UI or AudioEngine-connected Aether sample source; no claim of loop, slicing, mapped zones, streaming, or source replacement crossfade is made.

## Milestone C2A Aether sample source Slot 1 — 2026-07-13

Aether Slot 1 is now connected to Beat's existing project audio-asset decoder and cache. The stable synth patch fields are `aether.sample.1.enabled`, `audioFileId`, `rootNote`, `level`, `pan`, and `route`; synth patch schema v2 migrates schema-v1 patches with the slot disabled and empty. The project Aether object carries its own versioned `sampleSlot1` record. At this checkpoint a missing asset, empty ID, or unsupported future slot schema disabled only Slot 1; C3G1 later supersedes the future-version behavior with a stable diagnostic and rejected hydration so unknown data is not silently discarded. Slot 2 is implemented by C3E2; Slot 3 remains reserved and unimplemented.

`AudioEngine::rebuildSampleInstruments` resolves and decodes the selected project asset on the control/setup path, then publishes an immutable alias of the existing decoded cache buffer into each prepared Aether voice. No sample decode, file access, allocation, ownership destruction, pitch-table construction, or container growth occurs in `renderNextBlock`. Per-note playback shares the existing Aether envelope, deterministic stealing, and filter/direct lanes. Live and offline engines use the same project-application path. A Slot 1 identity change arms the accepted bounded route transition from the preceding output sample; it does not retain or overlap an old tail.

The product control lists only project audio assets already in Beat's audio-file store and exposes enable, asset, root note, level, pan, and route. Browser synthesis preview deliberately does not decode the asset through a second implementation; native Beat playback is authoritative. Focused native coverage proves Slot 1 persistence, decoded-asset playback, missing-asset silence, finite output, and live/offline WAV parity. Frontend coverage proves schema-1-to-2 migration, stable-field normalization, reverse conversion, and sample asset reference retention. The complete non-native suite passes. The unwaived native run still exposes only `baseline.recent-project-exists`; with the approved waiver every later native section, including C2A, passes. Production Beat, BeatBackendStress, and BeatAetherBaseline build.

The isolated baseline regenerated 150 WAVs with zero deadline overruns and unchanged queue telemetry of 64 accepted / 16 rejected / 16 overflow. Slot 1 defaults off, and this patch changes no file linked into BeatAetherBaseline, so the frozen scenarios remain outside the new path. The local report SHA-256 is `1b2c077cb39f29f65e4f55d794009ddc2685050686993eda579022fc6fc49c15`; its 150-file local manifest is `713ed72937dc82df0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`, peak RSS is 13,303,808 bytes, and measured render times are approximately 12.77–14.19 ms. These timing/RSS values are descriptive.

Looping, slicing, mapped zones/SFZ, streaming, per-source FX sends for the sample slot, granular playback, and spectral playback remain open. No upstream code, external dependency, factory sample, preset audio, branding, service, or licensing state changed.

## Milestone C2B Slot 1 slicing and forward looping — 2026-07-13

Sample Slot 1 now persists normalized start/end bounds plus an optional normalized forward-loop region. Synth patch schema v3 and sample-slot schema v2 migrate every earlier patch to full-range playback with looping disabled. Project, IPC, frontend, and native contract paths clamp ratios; reversed or out-of-slice loop bounds disable looping without rejecting an otherwise playable source. The decoded audio remains immutable and shared through Beat's existing cache.

`SampleSourceSlot::publish()` resolves ratios to integer sample bounds and prepares a bounded loop crossfade before any note can render. Note-on starts at the resolved slice start. Non-looping voices stop at the slice end through the existing 64-sample end fade. Looping voices use linear interpolation plus a fixed, at-most-64-sample seam crossfade and advance past the already-blended loop-start prefix after wrapping, avoiding both a seam step and duplicated prefix. No phase, envelope, modulation, preset timing, mapped-zone, streaming, granular, or additional-slot behavior changed.

Focused native coverage proves a nonzero slice starts on the expected decoded sample, a finite slice terminates, a valid loop remains active beyond its slice duration, note-off releases it, a reversed loop falls back to non-looping termination, replacement identity includes all five new fields, and warmed slice/loop rendering records zero allocation, blocking-lock, filesystem/stream, lazy-initialization, or container-growth violations. Live/export parity uses the same non-default slice and loop settings. Persistence and frontend round trips assert exact v2/v3 fields and legacy full-range defaults; the frontend interaction contract requires all five product controls.

The complete native suite passes with only `baseline.recent-project-exists` explicitly waived. An unwaived run still stops at exactly that persistence assertion (`order=1 existence=0 legacyTimestamp=1 remove=1 record=1`), so the waiver has not hidden an unrelated persistence failure. The complete non-native verifier and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. The regenerated matrix contains 150 WAVs and its normalized manifest is `ecb5269d7613c4dee94ff491741bc1b1a4a95c51130979280d6b9bb2463a048e`, byte-identical to a freshly normalized C2A manifest because every matrix scenario leaves Sample Slot 1 disabled. The timing-bearing C2B JSON SHA-256 is `5ae3f3933345ee6269be7ad993d4cadee76327d2a58a181d3274b538db4a077c`. The matrix completed in 2.37 s wall / 2.16 s user / 0.08 s system with 15,335,424-byte process maximum RSS (14,909,440-byte harness peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. No upstream code, runtime dependency, external asset, factory preset, branding, service, repository-wide license, or ownership declaration was introduced or changed.

## Milestone C2C bounded mapped sample zones — 2026-07-13

Sample Slot 1 now supports at most eight ordered zones, each referencing a Beat project audio asset and carrying root note, inclusive key range, inclusive velocity range, level/pan, slice bounds, and optional forward-loop bounds. Synth patch schema v4 stores zones in normalized Beat metadata; the project slot schema is v3. Older v1–v3 patches retain the exact C2A/C2B single-zone fallback. Missing zone assets are skipped during setup, future schemas disable the slot, and an empty map falls back to the stable single-zone fields.

`MappedSampleSourceSlot` owns eight fixed C2B child renderers. Publication validates and prepares immutable decoded-cache aliases off the callback. Note-on performs a bounded eight-entry scan, selects the matching zone with the narrowest combined key/velocity span, and uses stable array order to break ties. Exactly one child receives the note. Notes in an intentional key/velocity gap are rejected and counted; no fallback sample is invented. Rendering scans the fixed published zone count and preserves each zone's C2B slicing, looping, release, pitch, and pan behavior. Active map replacement is rejected, while project changes use the accepted route transition identity including every zone field.

The Aether editor can create a meaningful two-zone key split from the current sample, add/remove zones up to the hard cap, select project assets, and edit root/key/velocity bounds. “Use Single Zone” clears only the map and restores the C2B controls. Focused tests prove key selection, velocity selection, gap rejection, polarity-distinct zone output, mapped complexity, deterministic telemetry, active-publication rejection, persistence, schema migration, asset reference retention, and zero allocation/lock/file/lazy-init/container-growth violations around mapped rendering. The live/export parity fixture now uses two mapped zones through the real decoded-cache builder and retains missing-asset silence and bounded replacement behavior.

The full non-native suite, production `Beat`, `BeatBackendStress`, and the complete native suite pass with only `baseline.recent-project-exists` waived. The unwaived run still stops at exactly that known existence assertion. The regenerated baseline contains 150 WAVs; normalized manifest `ecb5269d7613c4dee94ff491741bc1b1a4a95c51130979280d6b9bb2463a048e` is byte-identical to C2B. C2C JSON SHA-256 is `12cabd7971ebf7049c1723c17afde7eaf53c8e4c95fb1a87d555e6e9453ff837`. The matrix completed in 2.15 s wall / 2.11 s user / 0.02 s system with 13,680,640-byte process maximum RSS (13,205,504-byte harness peak), zero deadlines, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. Round-robin, random selection, cross-zone blending, streaming, SFZ import, per-zone product slice/loop editing, and Slots 2–3 remain out of scope. No upstream code or dependency was introduced.

## Milestone C2D mapped-zone playback controls — 2026-07-13

Each of the eight bounded map entries now exposes its existing level, pan, normalized start/end slice, loop enable, and normalized loop-start/end fields in the Aether editor. Moving a slice start or end clamps the displayed loop region with it, loop-point fields are disabled while looping is off, and every numeric control retains the same bounds enforced by `normalizeAetherSampleZones` and the native C2C/C2B setup path. This is a product exposure of already-persisted schema-v4/v3 fields; it adds no parameter, migration, source type, render branch, dependency, or production DSP behavior.

The frontend interaction contract requires all seven per-zone playback controls plus the endpoint-clamping paths. Existing synth round-trip coverage preserves two zones with distinct level, pan, slice, and loop values through preview conversion. The complete non-native verifier, including typecheck, synth/DAW/document/interaction contracts and the production frontend build, passes. The complete native stress executable also passes every section with only `baseline.recent-project-exists` explicitly waived; the waiver still reports the exact disabled path-existence probe and does not hide later sections. Because C2D changes only Solid presentation, CSS, a source-contract test, and documentation, it cannot enter the native renderer or alter WAV output; the C2C 150-render manifest and telemetry remain authoritative. No upstream code, runtime dependency, preset, asset, branding, service, license, or ownership declaration changed.

## Milestone C2E Sample Slot 1 FX sends — 2026-07-13

Sample Slot 1 now has two stable, setup-rate send levels targeting the existing fixed Aether shared FX buses. Synth patch schema v5 and project sample-slot schema v4 default both levels to zero for every older patch. Frontend normalization clamps each send to 0–1 and project and IPC boundaries accept the frontend tuple and persisted scalar representation. This checkpoint's disabled-on-future behavior is superseded by C3G1: unknown future Slot 1 schemas now reject hydration with stable diagnostics. Product controls appear beside the existing oscillator, sub, and noise send controls. Send changes participate in the accepted sample-slot route identity bridge.

The voice renderer retains one fixed sample frame and adds it to each of the two preallocated source-bus accumulators only when the corresponding bus is configured and the sample send is nonzero. No container, source slot, effect graph, decode path, allocation, lock, file operation, lazy initialization, or quality fallback was added to the callback. A focused sample-only fixture proves a finite audible wet delta, muted-target output exactly matching dry, persistence of both levels, and zero allocation/lock/file/lazy-init/container-growth violations around a warmed real-time callback. Existing live/offline parity continues through the same engine path.

The complete non-native verifier and production build pass. Production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` build, and the freshly rebuilt complete native suite passes in 8.89 s wall / 7.61 s user / 0.60 s system with 199,327,744-byte maximum RSS and only `baseline.recent-project-exists` explicitly waived. The regenerated external matrix contains 150 WAVs and its filename-normalized manifest is `ecb5269d7613c4dee94ff491741bc1b1a4a95c51130979280d6b9bb2463a048e`, byte-identical to C2B/C2C because both new sends default to zero. C2E JSON SHA-256 is `940e0b5e6f2e5484f24d6766418021eae248789c0be92453c312eeb2e25ca323`. The matrix completed in 2.18 s wall / 2.14 s user / 0.02 s system with 15,171,584-byte process maximum RSS (14,745,600-byte harness peak), measured render times of 12.67–15.35 ms, zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. No upstream code, dependency, external asset, preset, branding, service, repository-wide license, or ownership declaration changed.

## Milestone C2F deterministic overlapping-zone crossfades — 2026-07-14

The approved overlap policy blends at most two matching zones. The bounded eight-entry scan retains C2C specificity ordering: lowest combined key/velocity span wins and stable array order breaks ties. The next most-specific match becomes the crossfade partner. Equal-power gains move across the inclusive key overlap according to the zones' key-range centres; when those centres coincide, velocity overlap is used. Zones with identical key and velocity bounds retain stable first-zone selection, preventing an accidental correlated gain increase. Gaps remain silent and notes matching only one zone are bit-exact with C2C–C2E behavior.

Crossfade gain is folded into each selected child note's existing velocity/gain scalar; no new per-sample curve state, allocation, container, branch by map size, schema field, or dependency is introduced. A logical note starts at most two of the eight fixed child slots, note-off still addresses every child by stable note ID, and telemetry continues to count one accepted logical event while child rendered-voice samples report actual work. Existing overlapping patches intentionally change from hard narrowest-zone selection to this blend; non-overlapping patches and identical duplicate maps do not change.

Focused native coverage proves key-overlap endpoints and midpoint equal-power arithmetic, velocity-overlap fallback after velocity normalization, a three-way overlap selecting exactly the two most-specific zones, stable identical-bound tie behavior, gap rejection, and zero callback-safety violations around the blended note path. The live/export fixture uses a real overlapping two-zone map through decoded-cache setup and remains within the established parity tolerance. The editor explicitly states that overlaps crossfade. The complete non-native verifier passes; production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` build; and the complete freshly rebuilt native suite passes in 9.72 s wall / 8.26 s user / 0.79 s system with 191,004,672-byte maximum RSS and only `baseline.recent-project-exists` explicitly waived.

The regenerated external matrix contains 150 WAVs and its filename-normalized manifest remains `ecb5269d7613c4dee94ff491741bc1b1a4a95c51130979280d6b9bb2463a048e`, byte-identical to C2E because the isolated scenarios do not use mapped sample overlaps. C2F JSON SHA-256 is `d04e17485b07bbd2a0b6d75e633abd9d9436ac72f5fb2faf0ea6b49f658d0d45`. The matrix completed in 2.44 s wall / 2.28 s user / 0.04 s system with 15,450,112-byte process maximum RSS (14,991,360-byte harness peak), measured render times of 12.79–17.77 ms, zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. No upstream code, dependency, schema, external asset, preset, branding, service, license, or ownership declaration changed.

## Milestone C2G bounded streaming cache foundation — 2026-07-14

The approved streaming architecture is a Beat-owned shared page cache rather than a sequential per-voice ring. A sequential ring cannot serve polyphonic voices reading unrelated source positions. `BoundedSamplePageCache` instead owns 16 resident pages of 4,096 stereo frames, two preallocated banks per resident slot, a fixed 64-request callback-to-worker SPSC queue, fixed request deduplication state, and atomic hit/miss/underflow/request/load telemetry. The cache does not own a file reader or thread. Setup code or one background consumer invokes the supplied loader; `readStereoFrame()` performs only bounded atomic scans and preallocated sample reads.

Page identity, bank identity, and a publication generation are committed in one release-store. A callback pins the selected bank, verifies the same descriptor, copies the frame, and releases the pin without waiting. The worker writes only an inactive bank with no readers. A miss returns silence to its caller and queues bounded work; queue saturation increments `requestsRejected`. The production sampler is not connected in this slice, so its existing decoded-buffer behavior and output are unchanged. The later connection must add the already-approved bounded fade-to-silence/recovery policy, a shared worker/file-reader owner, attack/loop preloads, and an explicit live-versus-offline ownership boundary before decoded long-sample buffers can be removed.

Focused native coverage proves callback cache hits and misses produce zero allocation, blocking-lock, file/stream, lazy-initialization, or container-growth violations; duplicate requests occupy one queue entry; saturation rejects work explicitly; requested pages materialize deterministic samples; invalid positions cannot enqueue work; and 200,000 randomized reads remain finite and coherent while a single worker repeatedly replaces pages. The complete native suite passes with only `baseline.recent-project-exists` waived, the complete non-native verifier passes, and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` build.

The external freeze regenerated all 150 WAVs. No render source differs from C2F, and the cache is disconnected. The explicit command `shasum -a 256 *.wav | sed 's#  .*/#  #' | sort | shasum -a 256` yields `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`; this is the same value previously recorded for C2A using that normalization. It does not reproduce the later undocumented `ecb5269d...` normalization, so the exact command is now recorded rather than silently treating unlike manifest procedures as comparable. C2G JSON SHA-256 is `b418ae3dd5afc8b1c1b361e2529e8543710379046d93d8ade4d6305b758a85b8`. The matrix completed in 2.16 s wall / 2.09 s user / 0.05 s system with 14,630,912-byte process maximum RSS (14,204,928-byte harness peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. No upstream code, runtime dependency, schema, external asset, preset, branding, service, licensing, or ownership state changed.

## Milestone C2H production long-sample streaming — 2026-07-14

The C2G cache is connected only for real-time Aether Slot 1 assets whose actual decoded-reader duration is at least eight seconds and whose project audio ID is not also referenced by an audio track/segment or whose path is used by the conventional sampler. Assets outside that conservative set retain the existing full decode. Offline/export engines explicitly select the full-buffer path regardless of duration. A project applied before a real-time device is prepared also remains fully decoded; this preserves correctness and merely forgoes the memory optimization until the project is reapplied.

One `SampleStreamingSession` owns at most eight file readers and one normal-priority background worker. Each asset has the fixed C2G cache plus a preallocated two-channel reader scratch buffer. The maximum page-bank payload is 1 MiB per asset (16 pages × 2 banks × 2 channels × 4,096 floats), or 8 MiB for the session before small descriptor/scratch overhead. Setup preloads the slice attack page, its successor, and the loop-start/loop-end pages before starting the worker. A callback miss requests a page and uses a 64-sample held-value fade to silence; playback stays silent until the fade completes, then a recovered page fades in over 64 samples. Playback position continues deterministically. The callback never calls the loader, wakes a condition variable, owns a thread, or destroys a reader.

`prepareForRealtime()` and `prepareForOffline()` now establish the mode explicitly. Session publication occurs with route state under the existing project lock; the previous worker is retained through route destruction and stopped outside that lock. Worker shutdown signals its wait and joins before reader/cache destruction. A control-side `SampleStreamingSnapshot` reports asset count plus aggregate hit, miss, underflow, request, page-load failure/defer, and queue-rejection counters.

Focused tests use a real nine-second WAV through the production project builder. They prove one Aether-only asset streams, the same asset is fully decoded when shared with a segment, explicit offline preparation never streams, offline live/export parity remains within the existing tolerance, output is finite/audible, preloaded and background-loaded pages are observed, and a warmed production device callback reports zero allocation, blocking-lock, file/stream, lazy-init, or container-growth violations. The standalone session test proves real worker file reads, deterministic recovery, an at-most-0.08 transition step, and callback-local instrumentation exclusion of worker I/O. The complete native suite passes with only `baseline.recent-project-exists` waived in 10.09 s wall / 8.08 s user / 0.89 s system with 217,907,200-byte peak RSS. The RSS includes several simultaneous test engines, full offline decodes, and a generated nine-second fixture; it is descriptive, not a production steady-state measurement.

The complete non-native verifier and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. The external matrix contains 150 WAVs and the explicitly normalized manifest remains `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`; frozen scenarios do not enable Sample Slot 1. C2H JSON SHA-256 is `0a81e0fe66d40b3dbe539bcf9a6e1fb90247b1c44e1ba218823e1d6091b2da09`. The matrix completed in 2.44 s wall / 2.17 s user / 0.06 s system with 15,876,096-byte process maximum RSS (15,368,192-byte harness peak), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. No upstream code, external runtime dependency, schema, preset, asset, branding, service, license, or ownership declaration changed.

## Milestone C2I sustained streaming-pressure gate — 2026-07-15

C2I changes only `BeatBackendStress` and the audit record; production streaming code, eligibility, cache/page budgets, worker count, schema, and output are unchanged. A deterministic test loader now holds each page operation for 2 ms, fails 64 consecutive loads, and receives 50,000 pseudo-random callback reads across 128 pages. The callback stays finite and coherent, missing reads return zero, the fixed request queue saturates explicitly, all 64 failures are observed, and a requested page recovers after the loader resumes. The timed accepted run measured 167.04 ms, 398 hits, 49,608 misses/underflows, 67 accepted requests, 24,464 rejected requests, 25,077 deduplicated requests, four successful page publications, and 64 failed page loads. These counts intentionally describe the deterministic fixture plus concurrent scheduling; correctness depends on bounded/nonzero relationships rather than an exact scheduler-sensitive count.

The real-file session fixture now uses a 48-page stereo WAV and adds an explicit sustained-gap transition check plus 16 simultaneous voices at divergent pitch rates through a forward-loop region spanning more pages than the 16-page resident cache. The gap reaches exact silence after the existing 64-sample held-value fade and recovers through the existing 64-sample fade without a step above 0.08. The timed polyphonic run rendered 81,920 output frames in 24.70 ms, remained finite/audible, observed 2,267,696 hits, 181,511 misses, 74 page loads, no queue rejection, and page replacement beyond resident capacity. Direct distant-page access covers seeking; the bounded loop and divergent rates cover repeated non-sequential churn. Both the synthetic starvation callback and every polyphonic render block report zero allocation, blocking-lock, filesystem/stream, lazy-initialization, or container-growth violations. Worker file reads remain outside the thread-local callback probe.

The complete native suite passes with only `baseline.recent-project-exists` explicitly waived in 9.53 s wall / 8.00 s user / 0.90 s system with 210,829,312-byte maximum RSS. Relative to the C2H recorded run this is 0.56 s and 7,077,888 bytes lower despite added tests; both measurements are descriptive single-machine observations, not a performance improvement claim. The complete non-native verifier and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass.

The external freeze regenerated all 150 WAVs. The explicitly normalized manifest remains `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`, proving this test-only gate changes no frozen render. C2I JSON SHA-256 is `0a5938ce5bfc4a8c442b25d59680ef23f9d7333f7b6d1f2b1202196d71d13c87`; render times were 13.03–16.02 ms (13.73 ms mean), harness peak RSS was 15,368,192 bytes, deadline overruns were zero, and queue telemetry remained 64 accepted / 16 rejected / 16 overflow. The only remaining waiver is the macOS-TCC recent-project existence probe; all unrelated persistence and later native sections remain enabled. C2I provides the required pressure evidence for the present conservative policy but does not authorize broader eligibility, larger caches, round-robin behavior, an external dependency, or SFZ import.

## Milestone C3A bounded SFZ subset parser — 2026-07-15

The approved SFZ strategy is a Beat-owned bounded subset with no upstream code or new runtime dependency. C3A adds only a playback-neutral parser/model and a control-thread file wrapper. It is not connected to project import, persistence, sample resolution, `AudioEngine`, `InstrumentVoice`, or any callback. Supported headers are `<control>`, `<global>`, `<group>`, and `<region>`. The normalized region model currently records relative sample path, key/root/velocity bounds, tuning, volume, pan, offset/end, continuous/sustain/one-shot/no-loop mode, loop points, group/off-by, sequence length/position, and attack/release trigger. Global/group inheritance and quoted or unquoted sample paths with spaces are deterministic; numeric notes and C4=60 note names are accepted.

Hard limits are 1 MiB of source text, 4,096 opcodes, 256 regions, and 512 stored diagnostics. Error and warning totals remain authoritative even when stored diagnostic detail is capped, so a zero-detail limit cannot turn malformed content into an accepted import. Absolute, home-relative, drive-qualified, URL-like, dot, and parent-traversing paths are rejected before any sample lookup. Unsupported headers/opcodes are explicit warnings and contribute no hidden behavior. Reversed key/velocity ranges, invalid notes/numbers, unsupported loop/trigger values, invalid offset/end or loop bounds, and sequence positions beyond their declared length are errors. Preprocessor directives, includes, master/curve/effect sections, and all unlisted opcodes remain unsupported.

Focused tests cover control/global/group inheritance, two regions, C3/B3/C4/C5 note conversion, quoted and unquoted spaced paths, loops, release trigger, groups, sequences, unsupported-opcode reporting, deterministic reparse, missing files, unsafe Unix/Windows/default paths, orphan opcodes, unsupported sections, every hard limit, malformed ranges, and diagnostic suppression. The selected-file wrapper reads only the `.sfz` text and does not require referenced sample files to exist at this stage. Production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` build. The complete native suite passes with only `baseline.recent-project-exists` waived in 9.19 s wall / 7.93 s user / 0.75 s system with 230,653,952-byte maximum RSS; the 19,824,640-byte increase from the C2I single-machine run is descriptive suite/process variance and not a production steady-state cost claim. The complete non-native verifier passes.

The external freeze regenerated 150 WAVs. Its explicit normalized manifest remains `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`, as expected for a disconnected text parser. C3A JSON SHA-256 is `93963bca90d4204845f61bdd4c262a5756e45197d73a229485d41df752cfec59`; render times were 12.85–22.50 ms (13.68 ms mean), harness peak RSS was 14,909,440 bytes, deadline overruns were zero, and queue telemetry remained 64 accepted / 16 rejected / 16 overflow. The cold first render accounts for the high timing endpoint; no quality threshold or output changed. C3B must add safe sample-root resolution and an immutable bounded region index before any playback connection. Sequence metadata is parsed but round-robin behavior remains separately gated.

## Milestone C3B bounded SFZ sample resolution — 2026-07-15

C3B adds a control-thread resolver between the disconnected C3A model and any future decode. Every referenced sample is canonicalized against the selected SFZ file's canonical parent directory and must remain inside that root component by component. Missing paths, non-regular files, symlink escapes, and non-audio extensions are errors. The allowlist is `.wav`, `.wave`, `.aif`, `.aiff`, `.flac`, `.ogg`, `.mp3`, and `.m4a`; this is only a resolution allowlist and does not claim that a later decoder supports every format. Limits are 4 GiB per file, 16 GiB aggregate unique sample bytes, 32 candidate regions per MIDI note, and 512 stored diagnostics. Error totals remain authoritative even when diagnostic storage is zero.

Successful resolution publishes an immutable shared instrument containing stable-order resolved regions plus a fixed 128-note candidate index. Duplicate sample references count once toward the aggregate byte budget. Sequence fields are carried as inactive metadata only. The resolver performs no decode, stream construction, project/schema mutation, route publication, or callback work. A later decoder must reopen and revalidate file identity and containment, because canonical-path validation alone cannot eliminate a time-of-check/time-of-use replacement between this control-thread pass and future decode.

Focused native coverage proves valid duplicate references and stable note indexing, sequence-metadata preservation without selection behavior, missing-file rejection, binary-extension rejection, real symlink escape rejection, per-file and aggregate byte caps, per-note candidate saturation, invalid limit rejection, parser-error rejection, missing-source rejection, and error visibility with a zero diagnostic-storage cap. The complete native suite passes with only `baseline.recent-project-exists` explicitly waived in 9.25 s wall / 8.04 s user / 0.77 s system with 203,554,816-byte maximum RSS. Full non-native verification and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass.

The external freeze regenerated all 150 WAVs. The exact normalized-manifest command recorded at C2G yields the unchanged `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`, byte-identical to C3A. C3B JSON SHA-256 is `bf9eb7dcd21ca3a1d5050b75b8d46f29b5bd604adcc0269f90a3a22563d29701`; the matrix completed in 2.28 s wall / 2.19 s user / 0.06 s system with 14,860,288-byte harness peak RSS, render times of 12.80–16.51 ms (13.75 ms mean), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. No upstream code, new dependency, external asset, schema, preset, branding, service, license, or ownership declaration changed. C3C remains blocked on a reviewed decode/publication design; round-robin remains a separate approval and behavior gate.

## Milestone C3C1 disconnected bounded SFZ decode — 2026-07-15

C3C1 adds a control-thread decoder that consumes only an accepted C3B instrument. It re-canonicalizes the sample root and every sample path, opens each unique file through a JUCE file stream, then repeats containment, canonical path, byte-size, modification-time, and opened-stream-length checks before handing the stream to Beat's existing JUCE format manager. Repeated region references share one immutable decoded buffer. Defaults allow at most 256 unique samples, two channels, 256 MiB decoded bytes per sample, 512 MiB aggregate decoded bytes, and 512 stored diagnostics; invalid limits and every over-budget condition fail before the corresponding audio-buffer allocation.

The immutable result preserves stable region order and the C3B 128-note index, but it is not published to `MappedSampleSourceSlot`, `InstrumentVoice`, `AudioEngine`, project state, or the streaming session. Focused tests use locally generated mono/stereo WAVs to prove deterministic decoding, duplicate sharing, sample-rate/channel/frame metadata, exact decoded-byte accounting, per-sample/aggregate/sample-count/channel limits, invalid-limit rejection, zero-detail error visibility, missing-source rejection, and changed-file rejection after resolution. The metadata checks substantially narrow the C3B time-of-check/time-of-use window but are not a native file-descriptor identity or cryptographic content proof; a same-size, same-timestamp adversarial replacement remains a publication blocker.

The complete native suite passes with only `baseline.recent-project-exists` explicitly waived in 9.13 s wall / 7.92 s user / 0.74 s system with 201,015,296-byte maximum RSS. Full non-native verification and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. The regenerated 150-WAV matrix retains normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`. C3C1 JSON SHA-256 is `623407d571fcd2935b75069a5897646bf7e21a07ffcbc04492b892f7ec353f5b`; the matrix completed in 2.24 s wall / 2.17 s user / 0.05 s system with 14,860,288-byte harness peak RSS, render times of 12.83–15.79 ms (13.55 ms mean), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow telemetry. No upstream code, dependency, external asset, schema, preset, branding, service, license, or ownership declaration changed. C3C2 must close file identity strongly and define lifetime-safe control-thread publication before any decoded SFZ reaches playback.

## Milestone C3C2 descriptor-level SFZ identity — 2026-07-15

The user approved the recommended Beat-owned descriptor policy. C3B now records each canonical regular sample's native device, inode, byte size, modification time, and change time alongside the portable metadata. On macOS and Linux, C3C opens the canonical path with `O_RDONLY | O_CLOEXEC | O_NOFOLLOW`, immediately `fstat`s the descriptor, and requires an exact match with the recorded native identity before format discovery. The same descriptor backs the JUCE reader. A second `fstat` after the complete decode must still match, detecting in-place mutation while decoding. Other platforms fail closed with `sfz.decode.identity-platform`; there is no silent fallback to pathname-only validation.

Focused release-stress coverage includes three development-only deterministic hooks that are not compiled into production `Beat`: replacing the target with a symlink immediately before open must fail through the `O_NOFOLLOW` detector, altering the recorded inode must fail the opened-descriptor match, and mutating the open file after reader completion but before the final check must fail the post-decode `fstat`. Existing path/containment, format, memory-budget, deduplication, deterministic-decode, and diagnostic-cap tests remain green. All opens, stats, hooks, allocations, reads, and destruction remain on the control/test thread; C3C2 is still disconnected from project import, source-slot publication, playback, streaming, and every callback.

The complete native suite passes with only `baseline.recent-project-exists` explicitly waived in 11.45 s wall / 8.19 s user / 1.08 s system with 212,549,632-byte maximum RSS. The higher wall/RSS values are descriptive single-run suite measurements after a full stress-target rebuild, not production steady-state cost. Full non-native verification and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. The regenerated 150-WAV matrix retains normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`. C3C2 JSON SHA-256 is `a4186208fd9a57581a085582852ee2846d49e213df6d10d67531b02f914d1b73`; render times were 12.89–16.43 ms (13.73 ms mean), harness peak RSS was 15,007,744 bytes, deadline overruns were zero, and queue telemetry remained 64 accepted / 16 rejected / 16 overflow. No upstream code, new dependency, schema, external asset, preset, branding, service, license, or ownership declaration changed. The remaining SFZ gate is lifetime-safe control-thread publication and deferred destruction; sequence selection remains separately unapproved.

## Milestone C3D1 dedicated indexed SFZ source slot — 2026-07-15

The approved architecture is a dedicated `SfzSourceSlot`, not a 256-entry expansion of the existing eight-zone map. It consumes only an immutable C3C2 decoded instrument. Candidate work occurs only at note-on through the fixed 128-note index and is capped at 32 regions; the sample loop visits only the fixed 16 voice records. The two most-specific overlapping key/velocity regions receive the same deterministic equal-power crossfade policy as Beat's bounded mapped sampler. Identical bounds retain stable-first behavior. A note requiring two overlap voices is rejected as a whole when two voice records are unavailable, preventing partial crossfades.

Each active voice pins its exact immutable publication bank for its entire lifetime. Control publication writes the inactive bank, atomically switches the published index, and retires the former owner only after its reader count reaches zero. Heavyweight shared-buffer destruction is therefore returned to the control thread through `takeRetiredInstrument()` and never occurs when a callback voice ends. Active replacement is deliberately rejected in this slice; this avoids an unreviewed audible swap policy while still making publication and retirement lifetime-safe under a note-on/publication race.

Supported playback fields are key/root/velocity, tune, volume, pan, inclusive offset/end, no-loop, continuous loop, sustain loop, and one-shot. Linear interpolation, a 64-sample start/end transition, bounded loop crossfade, and the existing 4 ms release policy prevent abrupt boundaries. One-shots ignore ordinary note-off; sustain loops exit their loop and release; continuous loops release in-loop. Sequence, release-trigger, group, and off-by regions fail publication explicitly rather than acquiring silent placeholder behavior. Decoded buffers are rescanned for finite samples at the control boundary. Dedicated counters expose candidate scans, voices started, capacity rejection, publication rejection, and retired publications alongside standard rendered-sample/voice work.

Focused native coverage proves 440 Hz tuning and +1200-cent octave tuning, pitch equivalence at 44.1/48/88.2/96/192 kHz, exact 64-versus-257 block equality, two-region stereo overlap, note-gap rejection, the 16-voice ceiling and deterministic 17th rejection, continuous/sustain/one-shot lifecycle behavior, replacement rejection while active, weak-owner lifetime through retirement, finite-buffer rejection, unsupported-semantic rejection, bounded candidate telemetry, and zero allocation/lock/file/lazy-init/container-growth violations around note-on and render. Production `Beat` compiles the slot through its own translation unit, but `AudioEngine`, project schemas, product import, and UI do not reference it yet.

The complete native suite passes with only `baseline.recent-project-exists` explicitly waived in 14.39 s wall / 12.11 s user / 1.54 s system with 199,950,336-byte maximum RSS. Full non-native verification and production `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. The final 150-WAV matrix remains byte-identical with normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`. C3D1 JSON SHA-256 is `93a8a3c4197f17d5794d990803da74fb3ea3ca337f5ae97a02b904e657815c39`; render times were 13.07–17.73 ms (14.81 ms mean), harness peak RSS was 15,335,424 bytes, deadline overruns were zero, and queue telemetry remained 64 accepted / 16 rejected / 16 overflow. Timing/RSS are descriptive single-run observations. No upstream code, dependency, schema, external asset, preset, branding, service, license, or ownership declaration changed. Product routing/import and active-source replacement remain later gates; sequence/round-robin remains separately unapproved.

## Milestone C3D2 managed SFZ product path — 2026-07-15

C3D2 connects the approved Beat-owned SFZ subset to Aether Sample Slot 1 without importing upstream code or adding an external runtime dependency. `instrument.importSfz` is a control/message-thread operation and requires a saved `.beat` project. The importer parses, resolves, and decodes the complete bounded instrument before writing anything; rejects sequence, release-trigger, group, and `off_by` semantics; hashes source and unique samples with SHA-256; copies only referenced files into hidden project-sidecar staging; verifies every copy; writes a versioned `beat.managed-sfz` manifest; and publishes with one directory move. Identical content reuses the stable content ID. Failed and unsupported imports leave no published asset.

Managed load rejects non-regular files, parent traversal, absolute child paths, symlinks, size/hash mismatches, malformed schemas, and decoder identity changes. Project schema 5 records the stable ID, display name, manifest, source, and sample paths. Save/package rewrites project-owned live paths to portable references, asset manifests and cleanup include every managed file, and reopening verifies before decoding off the callback. The UI exposes Import/Remove SFZ for Sample Slot 1 and reports unsupported semantics instead of approximating them.

`AudioEngine` publishes the verified immutable decode into `SfzSourceSlot`. A replacement sends new notes to the new synth while up to two previous synths receive tail-off and continue from pinned immutable instruments. Retirement storage is fixed-size, callback rendering uses a pre-existing empty MIDI buffer, and destruction occurs on a later control-thread rebuild; overflow hard-stops excess retirement and increments existing overflow telemetry. Sample-rate preparation and quality propagation cover current and retiring synths. This changes only explicitly enabled managed-SFZ projects.

Focused native coverage proves the saved-project guard, transactional import, deterministic reuse, portable packaging/resolution, traversal/symlink rejection, unsupported-semantics rollback, tamper detection, audible routing, finite output, and exact same-configuration render determinism at two block sizes. The final source-matched native suite passes in 9.03 s wall / 7.88 s user / 0.72 s system with 205,471,744-byte maximum RSS and only `baseline.recent-project-exists` waived. Full non-native verification covers schema-5 roundtrip, managed asset references/policy, IPC types, and the production build. Release production targets build. The unrelated `addAndMakeVisible` pointer correction in `Main.cpp` repairs a JUCE API compile error and has no DSP effect.

The external freeze regenerated 150 WAVs. Its normalized manifest remains `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`, byte-identical to C3D1. C3D2 JSON SHA-256 is `67633793acb9d66b31f61a32d32c06d432b5286150bfe56a58025e706f1cb75a`; render times were 12.47–15.90 ms (13.54 ms mean), harness peak RSS was 14,958,592 bytes, process peak RSS was 15,400,960 bytes, deadline overruns were zero, and queue telemetry remained 64 accepted / 16 rejected / 16 overflow. The only waiver remains the macOS-TCC recent-project existence probe. Slots 2–3, sequence/round-robin/random behavior, release triggers, group choking, automatic external relinking, and any upstream implementation remain out of scope and unapproved.

## Milestone C3E1 bounded granular source foundation — 2026-07-15

C3E1 adds a disconnected Beat-owned `GranularSourceSlot` behind the existing three-slot interface. It accepts one immutable, prevalidated decoded buffer and uses fixed arrays of eight note emitters and 32 grains. Source limits are two channels and 11,520,000 frames (60 seconds at 192 kHz); parameter limits are 2–1000 ms grain duration, 0.1–200 Hz density, ±48 semitones, and normalized position/spread/pan fields. Publication is control-only and refuses active replacement or non-finite/malformed input. No schema, UI, project route, file decoder, worker, or production callback references this slot yet.

Each emitter owns deterministic xorshift state derived from the patch seed, stable note ID, and MIDI note. Scheduling visits at most eight emitters per sample and rendering visits exactly 32 grain records. Grains use sample-rate-correct pitch increments, bounded linear interpolation, a Hann window with zero endpoints, equal-power randomized stereo placement, and fixed admission rejection when the pool is full. Active sample-rate preparation rescales scheduler time and grain window phase/duration and recomputes pitch rate without resetting position. Note-off stops future scheduling while existing grains finish; immediate stop clears both pools. Telemetry records scheduler ticks, grain starts/rejections, grain samples, accepted/rejected notes, and rendered work.

Focused native coverage proves exact 64/257 block equality, deterministic finite output at 44.1/48/88.2/96/192 kHz, active sample-rate changes without phase reset, the eight-emitter ceiling and ninth rejection, 32-grain pressure rejection, active-publication rejection, invalid-parameter and NaN-buffer rejection, complete tail retirement, bounded telemetry, and zero allocation/lock/file/lazy-init/container-growth violations around the pressured callback. The source-matched complete native suite passes in 10.34 s wall / 8.21 s user / 0.94 s system with 209,715,200-byte maximum RSS and only the existing `baseline.recent-project-exists` TCC waiver; full non-native verification and production `Beat`/`BeatBackendStress` targets pass. Timing and RSS are descriptive single-run suite measurements, not production steady-state cost.

The disconnected 150-render freeze remains byte-identical with normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`. C3E1 JSON SHA-256 is `76a1a336f3f97957e081206787e2b90084411caa69f40e582c0ef6ec2cd8d140`; render times were 12.73–16.00 ms (13.43 ms mean), harness peak RSS was 15,007,744 bytes, deadline overruns were zero, and queue telemetry was unchanged. Product routing, persistence, replacement crossfades, user parameters, source selection, resampling policy, and audible presets remain a later approval gate.

## Milestone C3E2 managed granular product path — 2026-07-15

C3E2 connects the bounded C3E1 engine as Aether Granular Slot 2. A saved project can import one WAV, AIFF, or FLAC source through `instrument.importGranular`. Import descriptor-decodes the source within the two-channel, 128 MiB decoded, 512 MiB encoded, and 11,520,000-frame limits before publication; hashes it with SHA-256; copies it into hidden content-addressed project-sidecar staging; verifies the copy; writes a versioned `beat.managed-granular` manifest; and publishes by one directory move. Managed load rejects traversal, absolute paths, symlinks, size/hash mismatches, schema mismatches, descriptor-identity changes, unsupported decoding, channel/frame overflow, and decoded metadata that differs from the manifest. Identical content reuses its stable asset ID.

Slot 2 persists stable fields for enable, built-in source, root note, level, route, position, position spread, grain duration, density, pitch, stereo spread, deterministic seed, two FX sends, and managed-asset provenance. Missing legacy slot data migrates to disabled; C3G1 later changes unknown future Slot 2 data to rejected hydration with stable diagnostics rather than silent disablement. Existing automation IDs are unchanged. These parameters are explicitly classified as control-thread source-rebuild parameters and are not realtime-modulatable in C3E2. A parameter/source change constructs the complete immutable source off the callback and changes the existing route identity. The already-bounded two-retiring-synth bridge therefore sends new notes to the replacement while old notes tail, and destruction remains on the later control-side rebuild. `InstrumentVoice` performs only fixed-pool granular rendering and routing in the callback.

The editor exposes import/remove, deterministic built-in benchmark selection, routing, root, level, position/spread, duration, density, pitch, stereo width, and seed controls. Factory preset `Benchmark - Granular Drift` uses a locally generated four-second stereo harmonic source and a quiet sine audition pilot; it contains no external sample or upstream content. Focused native coverage proves the saved-project guard, transactional import/reuse, traversal/symlink/tamper rejection, descriptor-backed bounded decoding, project-schema roundtrip, audible built-in rendering, finite output, repeat determinism at 64/257 blocks, fixed parameter policy, and the existing callback-safety gate. Frontend coverage proves stable parameter/asset roundtrip, migration defaults, factory presence/audibility, IPC typing, asset-reference ownership, and the production build.

The final source-matched native suite passes in 9.44 s wall / 8.07 s user / 0.81 s system with 208,863,232-byte maximum RSS and only `baseline.recent-project-exists` waived. Full non-native verification and the embedded-frontend Release `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` targets pass. The regenerated 150-WAV isolated matrix remains byte-identical with normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`. C3E2 JSON SHA-256 is `4bb4f28ce1ccab6deb7a81c47d3e8ad1ca39b497509a61a75be05b39fa9581b7`; render times were 12.62–15.78 ms (13.52 ms mean), harness peak RSS was 14,974,976 bytes, deadline overruns were zero, and queue telemetry remained 64 accepted / 16 rejected / 16 overflow. Timing and RSS are descriptive single-run suite observations. Slot 3 and spectral analysis/playback remain untouched.

## Milestone C3F spectral architecture review — 2026-07-15

`milestone-c3f-spectral-architecture-review.md` records the pre-implementation Slot 3 gate. An AI technical DSP review accepted Option B with revisions but explicitly did not provide specialist sign-off. The architecture now defines the square-root periodic Hann WOLA equation/scaling and -120 dB reconstruction gate; L/R magnitude and phase-residual storage; shared deterministic identity phase locking and transient decisions; canonical 48 kHz import/render resampling targets; bounded control-rate position and unresolved pitch-algorithm constants; complete 48 MiB accounting; named transform units and utilization gates; and separate event/alignment/resampler/reported latency contracts. No spectral code, schema, asset, dependency, product route, preset, callback edge, or render changed. C3F remains blocked pending human specialist confirmation and explicit implementation approval.

## Milestone C3G non-spectral hybrid closeout — 2026-07-16

C3G hardens only the completed Sample Slot 1 and Granular Slot 2 foundation. A shared persistence validator rejects malformed containers, over-capacity zone/path arrays, incomplete managed-asset records, unsupported built-in sources, missing enabled sources, invalid schema versions, and future Slot 1/2 or managed-asset versions with stable code/path/message diagnostics. `ProjectRepository::loadWithDiagnostics()` exposes the failure without returning a silently altered project. Sidecar cleanup runs the same validation before enumeration, reports a blocked result through IPC/Project Health, deletes zero files on uncertainty, and protects the complete directory of every referenced managed manifest. Valid current and supported legacy data remain compatible.

The Slot 1/2 editor now has labelled sections, live source status, described disabled switches, import busy state, benchmark pressed state, and deterministic focus after import/removal. The shared portal selector supports focus entry, Arrow Up/Down, Home, End, Escape, and focus return. Focused keyboard and source-contract tests plus the complete non-native suite pass. Attempted rendered browser inspection was blocked before Beat loaded by the browser runtime misparsing an unrelated workspace path containing a literal question mark; no live-browser result or new waiver is claimed.

The two user-requested factory string benchmarks have a dedicated test-only render freeze at 44.1/48/96 kHz. Two repeated two-second C4 renders per preset/rate are finite, audible, sustained, low-DC, below full scale, mutually distinct, and SHA-256 identical. The six reviewed hashes are enforced by `verify-aether-benchmark-renders.mjs`, which has no update mode and is part of `verify:non-native`.

Final source-matched Release `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` targets build. Full `verify:non-native` passes. The unwaived native suite stops only at `baseline.recent-project-exists`; the approved-waiver run passes every section in 9.41 s wall / 8.23 s user / 0.72 s system with 203,505,664-byte maximum RSS. The regenerated matrix has exactly 150 WAVs, normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`, timing-bearing JSON SHA-256 `7528de1d6f53c89e9c603d652d9365571f05e244610914b508886176022e8d3c`, 12.74–16.36 ms render time (13.61 ms mean), 15,450,112-byte process maximum RSS (14,974,976-byte harness RSS), zero deadline overruns, and unchanged 64 accepted / 16 rejected / 16 overflow queue telemetry. No existing baseline artifact was changed.

C3G introduces no production DSP change, Slot 3 behavior/schema, `SourceSlotIndex::three` change, or C3F review-package edit. Sample Slot 1 and Granular Slot 2 form the completed hybrid-source foundation. Milestone C and Slot 3 remain incomplete; C3F1–C3F3 remain paused pending qualified specialist conclusions and a later explicit approval.

## Milestone C3F1 disconnected spectral analyzer — 2026-07-16

The project owner explicitly waived the earlier specialist-credential prerequisite and authorized C3F1. The reviewer remains accurately recorded as AI technical assistance; the authorization record does not misstate reviewer identity. C3F1 adds only a disconnected Beat-owned analyzer and immutable artifact validator for already-verified, canonical 48 kHz in-memory PCM. There is no file import, worker launch, persistence/IPC field, `SourceSlotIndex::three` behavior, product route, preset, managed asset, playback source, or audio-callback caller.

Analysis version 1 uses a 1024-point transform, 256-sample hop, exact periodic square-root Hann windows, independent L/R magnitudes and float32 wrapped phase residuals, shared stereo peak/transient decisions, threshold 0.35, four-frame refractory interval, deterministic hashes, overflow-safe dimensions, a 5,625-frame ceiling, a corrected 1,439,232-sample/29.984-second fully overlapped input ceiling, and complete 48 MiB accounting. Validation fails on unknown schema/algorithm/window, malformed hashes or payload hash mismatch, inconsistent planes, non-finite/ranged values, phase bounds, invalid peaks/transients, Parseval energy mismatch, and cap overflow. Cancellation returns no artifact.

Focused native tests pass seven raw WOLA fixtures at or below -120 dB; the mixed stereo fixture measures -139.333 dB. The encoded artifact round trip measures -91.7453 dB and is explicitly not promoted to a playback-quality claim. C3F2 remains disconnected and unapproved until it resolves phase precision, identity phase-lock synthesis, pitch/resampling/latency constants, fixed callback work, and the measured realtime gates.

Full non-native verification and Release `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. The final native suite passes with only the existing TCC waiver in 9.92 s wall / 8.18 s user / 0.74 s system with 229,376,000-byte maximum RSS. All 150 default-disconnected WAVs remain byte-identical with normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`; C3F1 JSON SHA-256 is `2979d34da05091f33cccba4d54392d62d0719f7b1d55788cff1692644b1ff024`. Render times were 12.63–21.83 ms (13.60 ms mean), harness peak RSS was 15,138,816 bytes, deadline overruns were zero, and queue telemetry remained 64 accepted / 16 rejected / 16 overflow. No baseline artifact or waiver was changed.

## Milestone C3F2 artifact-v2 representation gate — 2026-07-16

Artifact schema/analysis v2 replaces accumulating float32 residuals with float64 evolution per detected L/R peak plus non-accumulating float32 phase relative to each bin's assigned peak. One-to-one predecessor matching is stable, bounded to four bins, and deterministic under ties. Assignment midpoint ties choose the lower peak; a lowest-bin global-maximum synthetic peak handles frames without local maxima. All phase values use canonical `[-pi, pi)` wrapping.

The two-second corpus covers stationary sinusoidal, multi-tone, harmonic, transient, noise, mono, stereo, and peak identity transitions. V2 reconstructs at -138.468 to -140.310 dB with RMS errors 1.33976e-9 to 4.63911e-8 and peak errors 1.19209e-7 to 2.08616e-7. Independently encoded float32 all-bin residuals measure -106.506 to -129.223 dB; independently encoded float64 all-bin residuals measure -138.662 to -140.171 dB. V2 improves every float32 case by 9.99–33.80 dB and remains within 0.208 dB of float64 all-bin storage, proving float32 relative phase is not the dominant error.

Exact decode/serialize roundtrips are byte-idempotent. Future versions, truncated/trailing payloads, excessive counts, hash mismatch, non-finite/ranged phase data, invalid/duplicate predecessors, malformed assignment/peaks, shape, energy, and 48 MiB overflow fail before any playback path exists. Worst-case materialized serialization measures 3,640 frames / 931,072 samples / 19.397333 seconds at 50,326,904 bytes; frame 3,641 is 50,340,730 bytes and exceeds the unchanged 50,331,648-byte budget. This supersedes C3F1's provisional 29.984-second limit.

C3F2 representation code remains disconnected: no product import/schema/UI/IPC, managed asset, Slot 3 behavior, offline-engine edge, or callback caller was added. Fixed-capacity playback is not implemented pending this report.

Full non-native verification and Release `Beat`, `BeatBackendStress`, and `BeatAetherBaseline` builds pass. The unwaived run stops only at `baseline.recent-project-exists`; the final approved-waiver run passes in 20.90 s wall / 17.36 s user / 1.59 s system with 262,553,600-byte test-process maximum RSS, including near-48-MiB materialization fixtures. Pre-sizing the exact serializer reduced the observed test peak from 801,849,344 bytes without changing serialized output or acceptance. The 150 WAVs remain byte-identical with normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`; JSON SHA-256 is `908572a538975a5582d14639df0c135dc37922d4a84a8970959c41d4f6467d37`. Render times were 12.98–15.75 ms (13.64 ms mean), harness peak RSS was 14,942,208 bytes, deadline overruns were zero, and queue telemetry remained 64 accepted / 16 rejected / 16 overflow. No new waiver or baseline update was introduced.

## Milestone C3F2 disconnected fixed-capacity playback — 2026-07-16

The user authorized the disconnected C3F2 playback and recommended active-position slices after accepting artifact v2. `SpectralSourceSlot` is a production-compiled but product-unreachable source implementation with four fixed voices; two fixed render lanes per voice; prevalidated immutable artifact ownership; fixed FFT, overlap, canonical, phase, and resampler storage; float64 identity-locked peak evolution; transient resets; direct bounded pitch scaling; explicit Nyquist discard/collision normalization; fixed setup position/freeze; active 5 ms position transitions; and a canonical-rate Kaiser converter. The complete fixed voice pool is allocated once in the constructor off callback to avoid a multi-megabyte stack object. A deterministic 1024-canonical-sample pre-roll distributes transform hops and prepares position targets across very small callbacks and is included in reported latency. Validation, table construction, allocation, and destruction remain off callback. No `AudioEngine`, Slot 3, schema, IPC, UI, managed asset, preset, file decode, or product route references the slot.

Focused results are 440/880 Hz for base/octave tuning; +0.00000467 dB 10 kHz passband delta; -129.147 dB worst 192 kHz image; -112.636 dB 44.1 kHz alias; -66.485 dB octave-up Nyquist discard; exact 64/257 block equality; fixed-position L1 difference 1112.92; active-position maximum jump 0.0218091; deterministic request supersession; active-rate-change continuity; zero synthesis underflows; and exact reported latencies of 1691/1792/3341/3631/7262 host samples across 44.1/48/88.2/96/192 kHz. Four voices pass and the fifth rejects. The full 200-cell extended matrix adds three active position requests to every nonzero-voice cell and measures worst P99.9 utilization 0.330492 and maximum 0.414492 with zero allocation/lock/file/lazy-init/growth violations.

Full non-native verification and embedded-frontend Release targets pass. The final source-matched native suite passes in 13.25 seconds wall / 11.59 seconds user / 1.02 seconds system with only the existing TCC waiver. Packaging creates the root app but local LaunchServices registration retains the unrelated `-10822` warning. The final 150-WAV default-disconnected matrix remains byte-identical with normalized manifest `713ed72937dc82df0a0d845ebff1d48aee8a8d87ab4af877cabc895c6bee5ba5`; JSON SHA-256 is `bf1fcd9fadccaf9126df19789a6ce800386bb36377e4c3b382080badd2c9ca75`, render times 12.80–15.77 ms (13.64 ms mean), harness peak RSS 15,171,584 bytes, zero deadlines, and unchanged 64/16/16 queue telemetry.

This does not close C3F2. Active position transitions and dynamic-position pressure are now closed for the disconnected engine; replacement at every overlap phase, sample-offset alignment, and product latency compensation remain open. C3F3 remains unstarted.
