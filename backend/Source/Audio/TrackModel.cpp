#include "TrackModel.h"

// Header-only data model — no out-of-line definitions yet.
// Kept as a .cpp file so we can attach JSON (de)serialization helpers here
// later without touching include sites.
